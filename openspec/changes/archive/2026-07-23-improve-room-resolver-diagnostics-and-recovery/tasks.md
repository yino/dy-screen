## 1. Rust 核心分类与安全诊断

- [x] 1.1 增加访问受限、未知布局、HTTP 入口失效和网络重试分类的失败测试与脱敏 fixture
- [x] 1.2 实现直播间 HTTP/页面分类并复用个人主页访问限制 marker
- [x] 1.3 增加 `inspect-room` 安全诊断命令、JSON 输出和 CLI 测试

## 2. 监听状态持久化与恢复

- [x] 2.1 增加 SQLite v5 migration 与旧开发版 v4 冲突修复测试，覆盖旧数据保留、默认失败次数、可空重试时间和 AI schema 恢复
- [x] 2.2 扩展主播 DTO、repository 和浏览器演示类型，持久化失败次数与下次重试时间
- [x] 2.3 增加 supervisor 分类测试，证明访问受限和页面变化不会清除 `web_rid`，只有明确入口失效参与三次恢复
- [x] 2.4 实现分类驱动的监听状态、退避时间和成功后诊断清零

## 3. 结构化日志与客户端诊断

- [x] 3.1 增加结构化日志测试，证明允许字段可见且页面、查询参数和 Cookie 不会写入
- [x] 3.2 实现按日 JSON Lines 文件日志、开发终端输出和 14 天清理复用
- [x] 3.3 增加前端测试，覆盖中文分类、错误摘要、连续失败、预计重试和操作入口
- [x] 3.4 实现主播表格诊断展示以及立即检查、打开直播间和打开日志操作

## 4. 文档与验证

- [x] 4.1 更新 README 和 Makefile，说明 `inspect-room` 用法、日志字段、分类含义和安全边界
- [x] 4.2 运行 Rust/前端目标测试、格式检查、Clippy、OpenSpec 严格校验和 Tauri 客户端构建

## 5. 线上验证码中间页回归

- [x] 5.1 增加新版“验证码中间页”的脱敏 fixture 和误分类回归测试
- [x] 5.2 扩展共享访问限制 marker，使 HTTP 200 验证码壳分类为 `access_restricted`
- [x] 5.3 运行解析器、supervisor、OpenSpec 和客户端构建回归

## 6. 添加主播错误原因透传

- [x] 6.1 增加直播间访问受限的 command 错误码、字段和中文原因回归测试
- [x] 6.2 同步 `streamer_service` 与房间校验辅助函数的分类映射
- [x] 6.3 运行服务层、前端、OpenSpec 和客户端构建回归

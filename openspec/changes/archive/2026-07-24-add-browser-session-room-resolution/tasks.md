## 1. 解析模型与安全诊断

- [x] 1.1 在 Rust 核心中增加浏览器页面快照、解析通道、访问分类、后续动作和逐次访问诊断 DTO
- [x] 1.2 把现有 Flight 解析拆成可复用脚本文本入口，保持直播、离线、未知结构和敏感 URL 脱敏行为一致
- [x] 1.3 增加浏览器快照大小、脚本数量、规范化 URL 和安全 marker 校验，拒绝非抖音来源与超限数据
- [x] 1.4 扩充 fixture 和核心测试，覆盖浏览器可解析页、访问验证页、未知结构、离线和敏感载荷不泄露

## 2. 双通道解析与状态机

- [x] 2.1 定义可测试的原生页面 resolver 与 `BrowserPageDriver` trait，以及浏览器会话状态快照和事件发布接口
- [x] 2.2 实现原生优先、访问受限回退、30 分钟粘性浏览器模式和单次原生试探状态机
- [x] 2.3 实现单浏览器请求队列、代次隔离、取消、15 秒有界页面探测及 `verification_required` 结果
- [x] 2.4 增加双通道状态机测试，覆盖成功、回退、冷却、恢复、超时、旧回调和并发请求串行化

## 3. Tauri WebView 会话

- [x] 3.1 实现 `TauriBrowserPageDriver`，按需创建持久化 `douyin-access` WebviewWindow 并默认隐藏
- [x] 3.2 限制 HTTPS 抖音导航、新窗口、下载和窗口 capability，使用 `eval_with_callback` 读取最小页面快照
- [x] 3.3 实现显示/隐藏验证窗口、访问状态查询、重新检查、清除 browsing data 和应用退出清理
- [x] 3.4 增加 Tauri fake driver 与集成测试，覆盖窗口状态、导航白名单、会话清除和无敏感 IPC

## 4. 监听、续录与日志接入

- [x] 4.1 让主播创建/编辑校验和 Supervisor 共用双通道房间解析服务，保留个人主页发现接管
- [x] 4.2 将全局公开页面访问门调整为至少 5 秒，将离线房间轮询改为 60 秒加 0 到 15 秒抖动
- [x] 4.3 接入 `browser_resolving`、`verification_required`、`session_ready` 和 `session_expired` 状态、通知及 worker 唤醒
- [x] 4.4 让 FFmpeg 结束后的地址刷新通过双通道 resolver，在同一逻辑会话中恢复录制并保留完成分片
- [x] 4.5 将每次原生访问、浏览器导航、探测、回退和最终动作同时输出到 stderr 与按日 JSONL
- [x] 4.6 增加监听与日志测试，覆盖多房回退、验证等待、立即检查、续录、取消和敏感字段白名单
- [x] 4.7 为原生 HTTP 与 WebView 导航补齐请求开始记录，确保请求卡住或返回前取消时仍可在控制台定位，并测试每个真实通道的开始与完成顺序

## 5. 客户端交互

- [x] 5.1 扩展 Tauri commands、`ClientApi` 和 TypeScript DTO，提供访问状态、立即验证、重新检查和清除会话操作
- [x] 5.2 在监控中心增加全局访问状态横幅、待处理数量、脱敏原因和不抢焦点的验证入口
- [x] 5.3 在设置页增加检查访问状态、重新验证和确认清除会话操作，并保持现有 AI 工作台修改不受影响
- [x] 5.4 增加主播行的浏览器解析/需要验证状态文案和操作，保持直播状态与监听状态分离
- [x] 5.5 让浏览器演示 API 返回稳定不可用状态，不创建真实窗口或伪造解析成功
- [x] 5.6 增加 React/API 测试，覆盖横幅、去重通知、验证操作、清除确认、状态恢复和演示降级

## 6. 多重验证与真实验收

- [x] 6.1 增加聚焦 Rust 核心、双通道服务、Tauri driver、Supervisor 和 React 的 Makefile 测试目标
- [x] 6.2 增加本地受支持页与访问验证页验收工具，证明每次访问均产生控制台和 JSONL 安全记录
- [x] 6.3 使用指定地址（下播时替换为当时在线且有权录制的公开房间）验证原生受限时 WebView 能确认直播并启动 FFmpeg 录制，记录实际 `web_rid` 与 `room_id`
- [x] 6.4 使用至少 3 个公开直播间连续监听 30 分钟，并验证一次流结束后的地址刷新与同会话续录
  - 2026-07-24 macOS 实机验收：6 个已启用公开直播间从 08:44 至 09:15 持续监听，4 个离线房间保持按周期检查，`24662340760` 与 `24157826171` 同时录制；前者连续录制超过 30 分钟并在同一会话内正常滚动分片，后者首次流结束后经 WebView 刷新为直播并在原会话内续录，第二次结束后刷新为离线并正常完成。全部已关闭分片均登记为 `complete` 且包含音频，验收结束时无孤儿或重复 FFmpeg 进程。
- [x] 6.5 运行格式、Clippy、Rust 全量测试、Tauri 测试、React 测试、前端构建和 `openspec validate --all --strict`

## 7. 文档、生命周期与交付审计

- [x] 7.1 更新 README 的双通道解析、访问验证、状态说明、会话隐私、日志字段、排障命令和非目标
- [x] 7.2 更新 Makefile 帮助、真实验收参数和控制台诊断命令，默认不把真实网络测试纳入普通 CI
- [x] 7.3 先增加实例锁互斥、释放后可重获和关闭流程只领取一次的失败测试
- [x] 7.4 在打开 SQLite 和执行 `reconcile_startup()` 前获取进程级独占锁，由 `AppState` 持有至应用退出，并输出脱敏启动诊断
- [x] 7.5 接管 Unix `SIGTERM`/`SIGINT`，与现有退出入口复用 20 秒总上限的幂等关闭流程，禁用会强制结束 Rust 进程的默认开发 watcher，并输出脱敏关闭诊断
- [x] 7.6 运行生命周期聚焦测试、格式、Clippy、Rust/Tauri/React 全量测试、前端构建和 OpenSpec 严格校验
- [x] 7.7 使用授权公开直播间验证第二实例无法修改活动会话，并验证 `SIGTERM` 后无孤儿 FFmpeg、重启后只有一个 worker 和活动会话
  - 2026-07-24 macOS 实机故障注入：`24157826171` 录制期间第二实例只输出 `instance_rejected` 并无 panic 退出，第一实例 PID、FFmpeg PID 和活动会话保持不变；显式 `SIGTERM` 在约 0.40 秒内依次输出 `shutdown_started` 与 `shutdown_completed`，会话保存为 `cancelled`，主播状态重置为 `waiting`，进程结束后无孤儿 FFmpeg；锁释放后通过 `tauri dev --no-watch` 重启，只恢复 6 个监听 worker、1 个活动会话和 1 个直属 FFmpeg。已确认 Tauri Rust watcher 会强制终止父进程，因此默认开发入口禁用该 watcher 并由自动化测试锁定。
- [x] 7.8 对照 proposal、design、四份 delta spec 和全部任务逐项审计完成证据，确认未覆盖用户的 ASR 与 AI 工作台改动
  - 审计结论：前 8 节 42 项任务均有自动化或实机证据；当前实现为每次公开页访问同时输出白名单控制台诊断并写入按日 JSONL，生命周期诊断不含 PID、本地路径或敏感流地址；单实例、无 panic 拒绝、SIGTERM 安全关闭、状态协调、no-watch 开发入口、重启单会话和含音轨录像均已验证。未修改 `add-user-triggered-ai-asr` 活动变更或其专属实现范围。

## 8. 验证会话隔离与卡死恢复

- [x] 8.1 先增加失败测试，复现旧验证目标阻断其他房间原生检查、手动检查不重新导航、重复 probe 刷屏和验证等待增加失败次数
- [x] 8.2 保留当前验证目标和浏览器页面，同时允许其他房间执行受节流保护的原生 HTTP 检查；其他房间同样受限时不得覆盖验证页
- [x] 8.3 让“检查访问状态”先重试当前验证目标的原生通道，仍受限时再执行新代次受控浏览器重载，并按状态变化与 30 秒心跳去重自动 probe 诊断
- [x] 8.4 验证等待不消耗连续失败预算，运行双通道、Supervisor、React、fixture、格式、Clippy、全量测试和 OpenSpec 严格校验

## 9. 验证码 iframe 灰屏修复

- [x] 9.1 使用真实受限直播间复现验证码中间页，确认标准浏览器会创建字节官方滑块 iframe，并先增加导航策略失败测试
- [x] 9.2 仅允许精确 `about:blank` 与 `rmc.bytedance.com/verifycenter/captcha/` 官方路径通过 WKWebView 导航回调，继续拒绝其他路径、相似域名、非 HTTPS、新窗口和下载
- [x] 9.3 运行 Tauri driver、双通道、格式、Clippy、全量测试和 OpenSpec 严格校验，并构建新的桌面客户端

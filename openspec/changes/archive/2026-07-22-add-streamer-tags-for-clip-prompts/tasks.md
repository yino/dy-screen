## 1. 标签领域模型与校验

- [x] 1.1 在 `src-tauri/tests/streamer_service.rs` 或新的标签测试文件中先添加失败测试，覆盖标签名称去空白、1–24 字符、可选指导 500 字符、控制字符、最多 10 个和同主播大小写无关去重
- [x] 1.2 在 `src-tauri/src/domain.rs` 定义 `StreamerTagInput`、`StreamerTag`、`StreamerPromptTag` 和 `StreamerPromptContext`，保持 Tauri 序列化为稳定 camelCase 字段
- [x] 1.3 实现共享标签规范化与校验函数，返回稳定中文错误码并避免在错误中回显完整非法标签内容
- [x] 1.4 添加标签、主播 DTO 和 `StreamerPromptContext` 序列化测试，确认无标签始终输出空数组且不会生成 Prompt 或模型请求字段

## 2. SQLite migration 与标签 repository

- [x] 2.1 在 `src-tauri/tests/repository.rs` 先构造当前版本数据库并添加 migration 失败测试，验证升级后已有主播、归档状态、监听开关、会话和视频关系完整保留且标签集合为空
- [x] 2.2 实施前检查主分支和其他活动变更的最新 migration 版本，在 `src-tauri/src/database.rs` 使用未占用版本新增 `streamer_tags` 表、级联外键及主播内名称和排序唯一索引
- [x] 2.3 添加 migration 幂等、外键检查、删除主播级联删除标签和旧版本数据库重复升级测试
- [x] 2.4 先添加原子替换失败测试，覆盖完整写入、重排、清空、唯一约束失败和中途写入失败时恢复旧标签集合
- [x] 2.5 实现 repository 的事务性标签整组替换、单主播标签加载和按 `sort_order, id` 的确定性排序
- [x] 2.6 添加主播列表和 dashboard 的批量标签加载测试，覆盖空列表、多主播、每主播多标签和禁止 N+1 查询的辅助边界
- [x] 2.7 实现一次批量查询加载标签并按 `streamer_id` 分组，保证所有主播 DTO 都包含 `tags`
- [x] 2.8 添加已有标签名称建议测试，覆盖跨主播去重、大小写规范化、数量限制和无数据结果
- [x] 2.9 实现本地标签名称建议查询，不创建全局标签实体，也不复用或覆盖其他主播的 Prompt 指导
- [x] 2.10 添加 `StreamerPromptContext` repository/纯函数测试，验证“带货”“搞笑”按优先级输出，无标签返回空数组

## 3. 主播创建、编辑和生命周期

- [x] 3.1 为 `create_streamer` 添加失败测试，覆盖个人主页和直播间来源携带标签、无标签、非法标签时不创建主播及不写入部分数据
- [x] 3.2 扩展 `CreateStreamerRequest`、`NewStreamer` 和创建事务，使主播和标签原子保存并在响应中返回规范化标签
- [x] 3.3 为 `update_streamer` 添加标签专用测试，验证只修改标签不会访问主页/直播间、不会停止或重建 worker、不会清除三层身份
- [x] 3.4 修改编辑服务，使名称、来源、监听开关和标签按现有顺序协调更新，并在任意失败时恢复原记录、标签和 worker
- [x] 3.5 添加归档与恢复测试，验证标签名称、指导和顺序随原主播保留
- [x] 3.6 添加延迟身份合并标签测试，覆盖不同名称取并集、目标指导为空、双方指导冲突和目标优先顺序
- [x] 3.7 添加合并后超过 10 个标签的测试，验证目标标签优先、来源按顺序补充到上限且合并仍成功
- [x] 3.8 在 `bind_discovered_room` 的同一事务中实现标签合并，任何失败必须保留两条主播及各自标签
- [x] 3.9 扩展 command 错误映射和中文字段错误，区分标签数量、名称、指导、重复和控制字符错误
- [x] 3.10 验证标签创建、编辑、归档、恢复和合并事件只刷新界面数据，不改变监听状态或启动 FFmpeg

## 4. Tauri 查询与未来上下文接口

- [x] 4.1 扩展主播列表、详情、dashboard 和创建/编辑 command DTO，使 Rust 与前端稳定传输 `tags` 数组
- [x] 4.2 增加只读标签名称建议 command，并验证返回值不包含其他主播的 Prompt 指导或内部敏感信息
- [x] 4.3 增加内部只读 `StreamerPromptContext` 查询接口或服务方法，不注册模型、Prompt、ASR、高光或切片后台任务
- [x] 4.4 添加 command 集成测试，覆盖标签排序、空数组、字段错误和上下文结构化输出
- [x] 4.5 检查事件、日志和错误脱敏，确认不会输出完整非法指导、签名直播流 URL、页面载荷或任何模型调用内容

## 5. React 表单、展示和浏览器演示

- [x] 5.1 扩展 `ui/src/types.ts` 和 `ui/src/api.ts` 的标签输入、标签 DTO、主播 DTO、建议查询及未来上下文类型
- [x] 5.2 在 `ui/src/App.test.tsx` 先添加表单失败测试，覆盖添加“带货”“搞笑”、删除、上移/下移、已有名称建议、数量限制和 backend 标签错误保持弹窗内容
- [x] 5.3 实现添加/编辑主播弹窗的标签项编辑器，支持名称、可选指导、删除和可访问的上移/下移按钮，不新增拖拽依赖
- [x] 5.4 在前端提供与后端一致的数量和长度即时提示，但提交结果和错误语义以后端校验为准
- [x] 5.5 添加主播表格和详情测试，覆盖多个标签徽章、无标签空状态、完整指导和窄窗口“+N”折叠
- [x] 5.6 更新主播表格与详情区域展示标签，确保直播状态、监听状态和主要操作在小尺寸窗口仍可访问
- [x] 5.7 更新浏览器演示 API 和 localStorage 数据迁移，模拟标签添加、编辑、排序、建议和空标签兼容
- [x] 5.8 在浏览器演示页明确标签仅为未来切片上下文预留，不生成 Prompt、不调用 LLM、不写入 SQLite
- [x] 5.9 验证主播事件刷新、合并后定位和编辑回填使用 backend 返回的最新标签顺序，不让旧事件覆盖新标签

## 6. 集成、文档和最终验证

- [x] 6.1 添加从创建带标签主播、重启数据库、编辑顺序、归档恢复到读取 `StreamerPromptContext` 的端到端测试
- [x] 6.2 添加无历史临时主页主播发现重复入口后的端到端测试，验证标签合并、临时记录删除和单 worker约束
- [x] 6.3 检查代码和依赖中未新增 LLM SDK、Prompt 模板、自动标签推断、ASR 调用、高光评分或切片实现，并确认不修改 `add-user-triggered-ai-asr` 活动变更
- [x] 6.4 更新中文 README，说明主播标签用途、数量限制、可选指导、当前仅作预留以及未来 LLM 可能重点提取“带货”“搞笑”等相关句子
- [x] 6.5 更新 Makefile 帮助和定向测试目标，使开发者可以运行标签 migration、repository、服务和前端组件测试
- [x] 6.6 运行 Rust 格式检查、两个 crate 的 Clippy、核心测试、Tauri 测试、前端测试和类型构建，确保监听、录制、视频库、预览和个人主页发现无回归
- [x] 6.7 运行 `openspec validate --all --strict`、Tauri release 构建和需求逐项对照，记录 Windows/Linux 标签界面尚待实机验证的边界

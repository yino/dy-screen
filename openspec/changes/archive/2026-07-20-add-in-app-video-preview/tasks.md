## 1. 预览领域模型与命令计划

- [x] 1.1 在 Rust 侧定义预览请求、任务状态、进度、媒体描述、稳定错误码和缓存清单模型，并为序列化行为添加测试
- [x] 1.2 实现 MP4 直接播放、MKV 重封装和 H.264 + AAC 回退转码的 FFprobe/FFmpeg 参数计划，并用有音轨、无音轨和中文路径用例测试参数映射
- [x] 1.3 实现包含规范路径、大小、修改时间和配置版本的 SHA-256 缓存键，并测试相同源复用及源变化失效
- [x] 1.4 实现 FFmpeg `-progress` 输出解析和阶段到百分比的映射，并测试已知时长、未知时长及异常输出

## 2. 缓存与后台任务服务

- [x] 2.1 新增独立 `PreviewService`，实现全局单任务 semaphore、请求去重、队列快照和关闭弹窗后继续执行
- [x] 2.2 实现缓存目录、JSON sidecar、最后访问时间、`.part` 临时文件、FFprobe 输出验证和原子发布
- [x] 2.3 实现启动及任务前清理，覆盖遗留临时文件、7 天过期、10 GB LRU 上限以及生成中或播放中条目保护
- [x] 2.4 实现转换前预计空间加 2 GB 安全余量检查，并确保失败时清理临时文件且不修改原始录像
- [x] 2.5 实现重封装失败后的受控转码回退、可用 H.264 编码器探测、显式重试和无无限重试错误处理
- [x] 2.6 实现源文件缺失时的缓存回查，以及应用退出时预览子进程停止和下次启动残留清理

## 3. Tauri 后端集成

- [x] 3.1 把 `PreviewService` 注入 `AppState`，注册请求预览、查询状态、重试和播放引用释放 command
- [x] 3.2 发送预览状态事件，并验证窗口隐藏或重建后可以通过主动查询恢复最新状态
- [x] 3.3 启用仅覆盖应用预览缓存目录的 asset protocol，并对数据库中验证通过的源 MP4 实施精确文件运行时授权和范围读取验证
- [x] 3.4 在视频及会话删除成功后清理对应预览缓存，在删除失败时保留原文件、业务记录和有效缓存
- [x] 3.5 保留并测试现有系统播放器打开和文件管理器定位 command，确保预览功能不改变原有行为

## 4. 前端播放器与双入口接入

- [x] 4.1 扩展 TypeScript 类型、Tauri API 和演示 API，支持预览请求、状态查询、重试及状态事件订阅
- [x] 4.2 实现共用的 `VideoPreviewDialog` 状态机，覆盖排队、探测、重封装、转码、可播放和失败状态
- [x] 4.3 实现播放/暂停、进度拖动、当前时间、总时长、音量、倍速、全屏和自动播放受阻后的手动播放
- [x] 4.4 在本次监听视频和历史视频库中把“预览”设为主要操作，同时保留系统打开、定位和删除操作
- [x] 4.5 实现缺失文件、缓存可用、空间不足、FFmpeg 缺失、损坏媒体和播放失败的中文反馈，并提供重试或系统打开入口
- [x] 4.6 确保关闭弹窗时暂停并释放媒体引用，且键盘焦点、按钮标签和窄窗口布局保持可访问

## 5. 测试与验收

- [x] 5.1 为缓存命中、任务去重、单任务排队、原子发布、过期/LRU 清理、磁盘保护和删除联动添加 Rust 自动化测试
- [x] 5.2 为两个预览入口、转换状态、缓存直达、自动播放回退、失败重试和播放器控制添加前端组件测试
- [x] 5.3 使用 H.264/AAC 可重封装 MKV、需要转码 MKV、无音轨视频、损坏文件、中文路径、大文件和重复点击样本执行 macOS 集成验收
- [x] 5.4 验证多直播间录制期间启动预览不会阻塞分片登记、状态事件或应用退出对账
- [x] 5.5 运行 Rust、前端、Tauri 构建及 OpenSpec 严格校验，并记录 Windows/Linux 尚待实机验证的边界

## 6. 文档与交付

- [x] 6.1 更新中文 README，说明内置预览、按需转换、缓存位置与清理规则、FFmpeg 依赖和系统播放器回退方式
- [x] 6.2 更新 Makefile 帮助和相关检查目标，使开发者能够验证预览所需环境、运行测试并构建客户端
- [x] 6.3 检查日志和界面错误不暴露直播签名 URL或不必要的本地路径，并完成最终回归清单

## 1. 会话级导入契约

- [x] 1.1 增加 service/repository 失败测试，覆盖主播当前直播时仍列出其历史已结束会话，并排除当前活动会话
- [x] 1.2 增加整场导入测试，覆盖按 `started_at ASC, id ASC` 展开全部登记分片且保留视频库 ID
- [x] 1.3 定义整场导入结果 DTO，包含更新后的项目详情以及新增、重复和不可用数量，并同步 Tauri command 序列化契约

## 2. 后端整场导入实现

- [x] 2.1 调整会话视频查询和导入服务，确保成员由后端按稳定顺序确定且再次校验会话已经结束
- [x] 2.2 实现相同视频库 ID 或源指纹的逐项幂等跳过，使部分重复不会阻止其余分片导入
- [x] 2.3 将缺失、损坏、未完成、不可读取或无音轨分片保留为不可用输入，并正确汇总导入结果
- [x] 2.4 增加部分导入后重试、陈旧活动会话请求和不可用分片不删除原文件的回归测试

## 3. AI 工作区交互

- [x] 3.1 更新前端 API 类型与适配器以接收结构化整场导入结果，并保持本地文件多选入口不变
- [x] 3.2 将历史直播入口明确为会话级选择，展示主播、起止时间、分片数、总时长和不可用数量，点击后直接导入全部分片且不打开文件或分片选择器
- [x] 3.3 导入后刷新统一输入列表并展示新增、重复和不可用数量，保持草稿阶段逐段删除和排序能力
- [x] 3.4 增加前端测试，覆盖当前直播不隐藏历史回放、一键加入全部分片、重复反馈以及导入后手动删除

## 4. 验证与文档

- [x] 4.1 更新 README 中“添加本地视频”和“添加整场直播”的边界，并说明历史可选、当前会话不可选及 ASR 仍遵守录制优先
- [x] 4.2 运行相关 Rust/Tauri 测试、前端测试、格式检查、Clippy、TypeScript 构建和 OpenSpec 严格校验

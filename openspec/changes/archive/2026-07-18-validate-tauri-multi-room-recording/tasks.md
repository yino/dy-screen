## 1. Rust 项目基础

- [x] 1.1 创建 Rust library/CLI crate、依赖清单、模块结构以及类型化错误和数据模型。
- [x] 1.2 添加脱敏后的 React Flight fixture，以及供 resolver 和 recorder 开发使用的测试辅助工具。

## 2. 抖音直播流发现

- [x] 2.1 为 URL 校验、React Flight 解析、直播流规范化、签名脱敏以及确定性的清晰度/协议选择添加失败优先测试。
- [x] 2.2 实现 HTTP resolver 和页面载荷解析器，直到直播流发现测试通过。

## 3. 录制核心

- [x] 3.1 为 FFmpeg 前置依赖检查、无 shell 命令构造、可选音频映射、分片和输出结构添加失败优先测试。
- [x] 3.2 实现 FFmpeg 进程监督、取消、完成分片收集和可序列化的逐房间结果。
- [x] 3.3 添加并发失败优先测试并实现 manager，使单房间故障与其他录制任务隔离。

## 4. CLI Demo 与 Tauri 边界

- [x] 4.1 实现直播流解析/列举和多房间录制 CLI，支持人类可读状态或 JSON-lines 状态。
- [x] 4.2 记录环境准备、多房间示例、输出产物、Tauri 2.0 集成边界以及延后的 ASR/NLP 工作。

## 5. 验证

- [x] 5.1 执行格式化、单元测试、静态检查、OpenSpec 校验以及 CLI help/错误路径烟雾测试。
- [x] 5.2 在网络和 FFmpeg 可用时执行有时间边界的真实直播间解析/录制烟雾测试，并明确记录环境限制。

## 1. 字幕领域与时间映射

- [x] 1.1 为剪辑工程详情增加只读字幕 DTO 和字幕完整性状态，并同步 Rust、Tauri 与 TypeScript 契约。
- [x] 1.2 从剪辑片段所属输入的已发布 ASR 产物查询非空规范化句段，按片段边界裁剪并映射到工程时间。
- [x] 1.3 增加句段完全相交、边界裁剪、跨来源重排、重叠确定性选择和缺失字幕的 repository/纯函数测试。

## 2. 受控字幕渲染与导出

- [x] 2.1 实现平台系统中文字体的后端受控选择、自动换行、透明背景字幕画面和 PNG 编码。
- [x] 2.2 生成只包含编号相对路径的字幕 `ffconcat` 序列，覆盖空白间隔、结尾、中文文本和毫秒时长。
- [x] 2.3 扩展剪辑 FFmpeg 导出计划，将字幕画面序列叠加到拼接视频，并使用平台受控 H.264/AAC 编码器。
- [x] 2.4 在保存对话框和 FFmpeg 前执行字幕完整性门禁，确保取消、失败、成功和关闭路径清理字幕与 MP4 临时文件。
- [x] 2.5 增加参数数组、前端不可注入、临时目录清理和真实 FFmpeg 带字幕小样测试。

## 3. 剪辑编辑器字幕预览

- [x] 3.1 在剪辑播放器中按工程播放头默认显示当前字幕，无命中时隐藏字幕背景。
- [x] 3.2 展示字幕完整性状态，在缺失时禁用导出并提供补全 ASR 或移除片段的中文提示。
- [x] 3.3 增加播放定位、跨片段切换、重叠选择、无字幕空白和导出门禁的前端测试。

## 4. 资源打包与交付验证

- [x] 4.1 扩展精简 FFmpeg 构建脚本和能力审计，加入 PNG、concat/image2、overlay 及剪辑导出所需滤镜和平台 H.264 编码器。
- [x] 4.2 更新 README 与 Makefile，说明字幕来源、默认样式、资源包升级、验证和已知边界。
- [x] 4.3 运行 Rust 目标测试、前端全量测试、真实 FFmpeg 小样、格式、Clippy、生产构建和 OpenSpec 严格校验。

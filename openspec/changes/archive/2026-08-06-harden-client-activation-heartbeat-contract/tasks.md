## 1. 授权契约与错误分类测试

- [x] 1.1 扩展 mock 客户端 API 服务，覆盖激活、同步心跳、周期心跳的 `200/201`、`1001~1007`、`2001~2004`、`9000`、`9101~9105`、超时、网络失败和无效响应
- [x] 1.2 为错误处置矩阵增加表驱动测试，验证确定失效、请求头契约异常、可重试错误和有界协议恢复不会互相混淆
- [x] 1.3 增加响应关联头测试，验证有效和无效正文场景均能采集 `x-request-id` 与可选 `x-trace-id`

## 2. 系统安全凭据存储与迁移

- [x] 2.1 定义可替换的 `ActivationSecretStore` 接口、生产平台适配和测试内存 fake，生产构建禁止回退 SQLite 明文
- [x] 2.2 将完整设备 ID、激活码和离线 token 的读写、覆盖与幂等删除迁移到 macOS Keychain/Windows Credential Manager
- [x] 2.3 新增 SQLite migration，将授权表收敛为非秘密元数据，并按“写安全存储、回读验证、事务清除旧秘密”迁移既有记录
- [x] 2.4 增加新激活、旧数据升级、中断恢复、安全存储不可用、删除失败和重复清除测试，确认不会丢失唯一凭据或错误解锁主界面

## 3. 请求头与脱敏控制台诊断

- [x] 3.1 收敛统一请求头构造器，为所有需授权请求同时发送 `device_id`、`activate_code`、`device-id` 和 `activate-code`
- [x] 3.2 在 `ApiClient` 增加白名单诊断 DTO，输出激活/心跳请求阶段、固定路径、耗时、HTTP 状态、业务码、授权状态和关联 ID
- [x] 3.3 为其他客户端 API 增加仅失败摘要，并确保任何路径都不 Debug 输出原始 `HeaderMap`、请求/响应正文或完整 URL
- [x] 3.4 增加日志捕获和敏感信息回归测试，断言完整设备号、激活码、token、Cookie、签名、绝对路径和测试秘密均不出现在控制台输出

## 4. 激活后的同步心跳确认

- [x] 4.1 为授权状态增加 `verifying`、`contract_error` 及稳定错误详情，并以单飞锁和请求代次防止旧激活或心跳回写新状态
- [x] 4.2 将激活流程改为激活响应成功后立即同步心跳，只有 `code=200`、`revoked=false`、`state=ACTIVE` 才原子发布 `active`
- [x] 4.3 实现确定卡密/设备失效、请求头契约异常、暂时错误和 `9101~9105` 有界恢复的完整状态转换
- [x] 4.4 调整 Tauri command 和后台心跳生命周期，确保 Supervisor、AI runtime 和素材同步只在最终确认后恢复且强制下线时幂等暂停
- [x] 4.5 增加并发激活、同步心跳失败、周期心跳与手动激活竞争、重复事件和后台任务只恢复一次的集成测试

## 5. 前端独占激活门禁

- [x] 5.1 同步 Rust DTO 与 TypeScript 类型，移除开发免激活状态并支持 `verifying`、`contract_error` 和下一次重试信息
- [x] 5.2 更新激活页交互，在同步心跳期间显示验证进度并禁用重复提交，暂时失败时保留输入，确定失效时要求重新输入
- [x] 5.3 为卡密失效、设备异常、请求头/代理异常和网络重试提供不同的中文安全提示，不展示任何授权秘密或原始响应
- [x] 5.4 增加前端测试，验证心跳确认前及强制下线后主导航、监控、视频库和 AI 工作区均未挂载，并消除首页闪现

## 6. 文档与验证

- [x] 6.1 更新 README 和配置说明，记录明文 JSON 边界、正式 HTTPS 要求、双请求头兼容策略及 Nginx `underscores_in_headers on;` 部署检查
- [x] 6.2 运行授权 repository、API 契约、Tauri 生命周期和前端门禁目标测试，并执行 TypeScript 类型检查、Rust fmt 与 Clippy
- [x] 6.3 运行 `make check`、`openspec validate --all --strict` 和 `git diff --check`，核对日志、测试 fixture、文档和提交差异均不包含真实激活码、设备 ID、token 或其他秘密
- [ ] 6.4 使用配置正确的开发服务端完成“激活、同步心跳、进入首页、周期续约、服务端禁用、重新激活”实机验收，并通过 `x-request-id`/`x-trace-id` 关联客户端与后端日志

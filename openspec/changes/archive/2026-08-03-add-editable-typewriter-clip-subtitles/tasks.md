## 1. 数据模型与迁移契约

- [x] 1.1 先增加从现有数据库升级的失败测试，覆盖新字幕表、外键、唯一约束、幂等 migration、原工程和原 ASR 数据保留
- [x] 1.2 增加 `ai_clip_subtitles` additive migration 和必要索引，确保片段删除级联且不删除录像、ASR、高光或已导出文件
- [x] 1.3 扩展 Rust 领域模型、Tauri DTO 和 TypeScript 类型，区分工程字幕原文、当前文本、隐藏状态、工程时间及后端显示帧
- [x] 1.4 增加字幕更新与恢复 command 契约测试，拒绝前端提交时间戳、原文、字体、路径、滤镜或 FFmpeg 参数

## 2. 工程字幕副本与编辑事务

- [x] 2.1 先增加 repository 失败测试，覆盖新工程建立副本、旧工程幂等回填、追加片段、重排、移除和无有效句段时的不完整状态
- [x] 2.2 实现新建、追加和旧工程首次打开时的事务化字幕副本投影，并让后续 ASR 发布及其他工程编辑不覆盖当前副本
- [x] 2.3 先增加字幕编辑失败测试，覆盖文本规范化、500 字素上限、空文本、隐藏恢复、恢复原文、跨工程 ID 和过期工程版本
- [x] 2.4 实现带 `expectedProjectVersion` 的字幕更新事务，递增工程版本并返回最新工程详情
- [x] 2.5 实现导出期间拒绝编辑，以及已完成工程保存字幕后保留旧文件但把工程恢复为需要重新导出的状态
- [x] 2.6 调整字幕完整性规则，使缺少字幕来源继续阻止导出，而用户主动隐藏已有字幕仍允许导出

## 3. 逐字显示与长句分页计划

- [x] 3.1 先增加纯函数失败测试，覆盖中文、组合字符、emoji、空白、前后标点、单字句、85% 边界、句段空白和确定性重叠选择
- [x] 3.2 引入受控 Unicode 字素切分依赖并实现显示单元分组，确保标点不单独消耗逐字步长
- [x] 3.3 先增加长句失败测试，覆盖横屏和竖屏安全宽度、最多两行、稳定换行、多页时间分配、不截断和最小字号
- [x] 3.4 实现基于受控字体与输出尺寸的分页器，以及包含完整页面、可见前缀、隐藏后缀和半开时间范围的权威显示计划
- [x] 3.5 把显示计划接入工程详情、片段重排、字幕编辑和输出尺寸更新，并为无输出尺寸的首次打开提供确定性默认值

## 4. 无背景字幕渲染与安全导出

- [x] 4.1 先扩展字幕 PNG 和 FFmpeg 导出测试，验证透明背景、黑色描边、轻微阴影、白色字形、逐字帧、分页、隐藏字幕和临时目录清理
- [x] 4.2 重构 Rust 字幕渲染器复用权威显示计划，移除矩形背景填充并实现固定描边、阴影、安全边距和最多两行样式
- [x] 4.3 让导出任务冻结工程版本和显示计划，继续通过受控参数数组烧录，并在版本不一致、取消、失败和异常退出时安全终止与清理
- [x] 4.4 增加多来源、重叠句段、全部主动隐藏、缺少字幕来源和旧工程回填后的真实 FFmpeg 集成测试

## 5. 剪辑工作台字幕编辑体验

- [x] 5.1 先增加 React 失败测试，覆盖字幕轨、字幕点击定位、右侧模式切换、当前片段过滤、工程搜索和当前播放字幕高亮
- [x] 5.2 扩展前端 API 适配器和浏览器测试替身，支持字幕保存、隐藏恢复、恢复原文、版本冲突和最新工程详情刷新
- [x] 5.3 在视频轨下增加可横向滚动且与缩放、播放头一致的字幕轨，隐藏字幕使用弱化状态且禁止拖动调时
- [x] 5.4 在右侧属性区增加“片段 / 字幕”分段控件、可滚动列表、搜索、匹配计数和定位联动，避免列表滚动带动整个页面
- [x] 5.5 实现中文输入兼容的字幕草稿，支持失焦或 `Cmd/Ctrl + Enter` 保存、`Escape` 放弃，并展示未保存、保存中、已保存、失败和冲突状态
- [x] 5.6 实现隐藏、恢复显示和恢复 ASR 原文操作，保存后保持当前片段、播放位置、字幕列表和字幕轨选择
- [x] 5.7 修改播放器字幕叠层，使用后端页面与可见前缀实现稳定逐字展示，去掉背景并应用与导出一致的白字、黑描边、阴影和安全位置
- [x] 5.8 在导出期间锁定所有改变工程版本的控件；字幕修改后更新旧成品过期提示和重新导出主操作

## 6. 回归验证与文档

- [x] 6.1 更新剪辑字幕和工作台测试夹具，确保 AI 工作区原始 ASR 浏览仍为只读且不显示工程修正文案
- [x] 6.2 运行 Tauri repository、command、字幕计划、渲染和剪辑导出目标测试，以及可用环境下的真实 FFmpeg 集成测试
- [x] 6.3 运行前端 AI 工作区测试、完整前端测试和生产构建，检查横屏、竖屏、长字幕及长列表布局
- [x] 6.4 运行两个 Rust workspace 的格式、Clippy 和完整测试，执行 `git diff --check` 并确认没有原始媒体、ASR 文本样本或本地敏感路径泄露
- [x] 6.5 更新 README 中的字幕校对、逐字显示、固定样式、项目隔离和重新导出说明，并执行 `openspec validate --all --strict`

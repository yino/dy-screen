## 1. 客户端工程基础

- [x] 1.1 将根 `package.json` 更新为 React + TypeScript + Vite + Tauri 2.0 项目，并创建前端入口、TypeScript/Vite 配置和基础测试配置。
- [x] 1.2 创建 `src-tauri` crate、Tauri 配置、权限文件和应用图标占位，依赖根 `dy-screen` crate。
- [x] 1.3 更新 `.gitignore` 和 Makefile，覆盖前端依赖、Tauri 构建、开发、测试和全量检查目标。

## 2. SQLite 数据层

- [x] 2.1 先编写 migration/repository 失败优先测试，覆盖首次建库、重复迁移、主播去重、监听开关和设置默认值。
- [x] 2.2 实现 SQLite 连接、版本化迁移以及 streamers、recording_sessions、videos、settings 和 schema_migrations 表。
- [x] 2.3 实现主播、会话、视频和设置 repository，并保证短事务、类型化错误和签名 URL 脱敏。
- [x] 2.4 实现启动对账：关闭遗留活动会话、登记完成分片和标记缺失文件。

## 3. 监听与自动录制

- [x] 3.1 先编写监听状态机测试，覆盖恢复监听、30 秒检查、错误退避、单 worker 去重和直播/监听双状态。
- [x] 3.2 实现 supervisor 和逐主播 worker，支持立即检查、暂停/恢复单个、暂停/恢复全部和系统恢复触发。
- [x] 3.3 先编写自动录制测试，覆盖开播创建会话、下播关闭、关闭监听停止、并发上限和磁盘阈值。
- [x] 3.4 接入根 resolver/recorder，实现自动录制、完成分片落库、最多 3 次重新解析续录和会话事件。
- [x] 3.5 实现默认并发 4、等待资源状态、10/2/1 GB 磁盘保护和本地通知触发边界。

## 4. Tauri command 与桌面生命周期

- [x] 4.1 实现主播 CRUD、状态刷新、会话/视频分页、设置和文件操作 Tauri command。
- [x] 4.2 实现状态变更 Tauri event，使前端可以增量刷新主播、摘要、会话和视频。
- [x] 4.3 实现关闭窗口隐藏、托盘菜单、显式退出有界清理以及活动录制数展示。
- [x] 4.4 实现通知、打开视频/目录、日志目录、可选开机启动和 FFmpeg/FFprobe 诊断。

## 5. React 客户端界面

- [x] 5.1 先编写前端组件测试，覆盖空状态、表单错误、状态排序、导航和 AI 占位页。
- [x] 5.2 实现参考图方案 A 的应用框架：浅色/深色主题、绿色主色、左侧导航、顶部摘要和响应式布局。
- [x] 5.3 实现主播监控表格、直播/监听状态、操作菜单、添加/编辑弹窗和右侧本次视频详情。
- [x] 5.4 实现历史视频库的主播/日期/状态筛选、50 条分页、文件状态和确认删除交互。
- [x] 5.5 实现设置页、环境诊断、托盘说明、日志入口、开机启动开关和 AI 剪辑规划中页面。

## 6. 文档与验证

- [x] 6.1 更新中文 README，说明客户端安装、开发、SQLite/录像位置、后台监听、FFmpeg 环境和已知限制。
- [x] 6.2 执行前端格式/类型/测试/构建，以及根 crate 和 `src-tauri` 的格式、Clippy 和测试。
- [x] 6.3 执行 Tauri build 或平台可用的等价检查，并验证应用能够创建数据库、添加主播和恢复监听。
- [x] 6.4 运行 OpenSpec 严格校验，记录无法在当前环境完成的系统通知、开机启动或真实录制限制。

## 7. 审查修复与可靠性交付

- [x] 7.1 先编写失败优先测试并实现 `segments.csv` 增量监听，完成分片在 FFmpeg 运行期间即时上报，携带时间、时长和音频元数据，退出时最终扫描且不重复发送。
- [x] 7.2 先编写数据库回归测试并实现 SQLite 忙等待、归档主播恢复、视频服务端筛选分页、外部缺失同步，以及仅在分片写库成功后发布刷新事件。
- [x] 7.3 先编写 supervisor 回归测试并重构同一直播周期的续录边界，使 FFmpeg 退出、解析瞬时失败、重试等待和并发许可均可取消且最多连续重试 3 次。
- [x] 7.4 先编写退出时序测试并实现统一取消、全局截止时间、超时终止、磁盘监控联动取消和遗留会话对账。
- [x] 7.5 先编写 command/repository/前端测试并实现添加主播真实访问校验、按会话分组展示以及删除整个会话的失败回滚和危险确认。
- [x] 7.6 更新中文 README、Makefile 和忽略规则，执行格式化、前后端测试、Clippy、客户端构建、OpenSpec 严格校验和最终代码复审。

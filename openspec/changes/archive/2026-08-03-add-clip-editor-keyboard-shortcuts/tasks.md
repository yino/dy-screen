## 1. 快捷键行为测试

- [x] 1.1 在 `ui/src/AiWorkspace.test.tsx` 增加 `Space` 播放/暂停及自动重复保护测试
- [x] 1.2 增加左右逐帧、允许方向键重复、跨片段定位和固定 30 FPS 步长测试
- [x] 1.3 增加 `Shift + ArrowLeft/Right` 五秒跳转、`Home` / `End` 首尾定位、工程边界限制和播放状态保持测试
- [x] 1.4 增加 `M` 静音切换、重复保护以及 `+`、`=`、`-` 每次 5 个百分点缩放和 `1–100%` 边界测试
- [x] 1.5 增加字幕文本域、搜索框、滑块、按钮、可编辑元素、组合输入及 `Ctrl/Meta/Alt` 修饰键不触发全局快捷键的测试
- [x] 1.6 增加离开剪辑页或组件卸载后键盘监听已清理的测试

## 2. 剪辑工作台快捷键实现

- [x] 2.1 在 `ClipEditor` 中实现可复用的事件目标保护判断，覆盖表单控件、按钮、可编辑元素、组合输入和额外修饰键
- [x] 2.2 在 `ClipEditor` 生命周期内注册并清理 `keydown` 监听，只对已处理快捷键阻止浏览器默认行为
- [x] 2.3 将 `Space`、逐帧、五秒跳转、首尾定位和 `M` 映射到现有播放器及工程时间控制路径，并实现重复按键规则
- [x] 2.4 实现 `+`、`=`、`-` 的 5 个百分点时间轴缩放及 `1–100%` 限制，确保不修改工程数据或播放头
- [x] 2.5 更新播放/暂停、上一帧、下一帧、静音和时间轴缩放控件提示，标注对应快捷键且不改变布局

## 3. 回归验证

- [x] 3.1 运行 `npm test -- --run ui/src/AiWorkspace.test.tsx` 并确认剪辑、字幕编辑和快捷键测试全部通过
- [x] 3.2 运行 `npm run build` 和 `make check`，确认前端类型、构建及全项目回归无新增失败
- [x] 3.3 运行 `openspec validate --all --strict` 和 `git diff --check`，确认规格与修改格式有效

## 1. 阈值与精彩候选持久化

- [x] 1.1 为 LLM 设置和精彩运行增加合格/优秀阈值字段、默认值、迁移回填、幂等与完整性测试。
- [x] 1.2 扩展 Rust/Tauri/TypeScript 设置 DTO 与保存校验，拒绝范围外或优秀阈值低于合格阈值的请求。
- [x] 1.3 将冻结阈值纳入精彩分析指纹与运行快照，保证设置变更不重写历史运行语义。
- [x] 1.4 修改评分发布逻辑以保存全部有效候选、取消前 10 条限制、首次发布自动选择优秀候选并保留后续人工选择。
- [x] 1.5 实现按运行合格阈值筛选、稳定排序和分页的候选查询，并覆盖大量候选、低分保留、边界分数和缓存恢复测试。

## 2. 剪辑工程与安全导出核心

- [x] 2.1 定义剪辑工程、片段、内置动画/转场、导出状态与进度 DTO，并创建 SQLite 表、外键、索引和迁移测试。
- [x] 2.2 实现从已选择候选确定性创建或恢复默认工程、候选快照、重建保护、片段重排、移除和属性更新 repository/service。
- [x] 2.3 实现工程来源重新校验、输出规格探测和工程时间到来源视频时间的映射，拒绝缺失或未授权输入。
- [x] 2.4 实现受控 FFmpeg 导出计划：源内裁取、规格规范化、音量、白名单动画/转场、H.264/AAC concat、无 shell 拼接与参数数组测试。
- [x] 2.5 实现单并发、可取消的导出服务、进度事件、临时 MP4 清理、原子发布、磁盘/目标错误分类和关闭等待。
- [x] 2.6 增加 Tauri commands 与保存对话框：打开/更新工程、启动/取消/查询导出，确保前端不能提交路径、滤镜或 FFmpeg 参数。
- [x] 2.7 增加同一精彩运行待切片候选的受控追加与指定位置插入命令，覆盖归属、选择状态、重复与顺序校验。

## 3. 精彩工作区与独立编辑器界面

- [x] 3.1 在设置页增加合格/优秀片段阈值控件、默认值说明、实时前端校验和后端错误反馈。
- [x] 3.2 更新 AI 精彩视图：显示冻结阈值、只导航合格候选、显示完整统计、自动选择反馈和“编辑视频”入口。
- [x] 3.3 新建独立剪辑编辑页面与返回状态，复用当前应用主题和播放器预览，不将编辑器嵌入卡片或分析工作区。
- [x] 3.4 将剪辑页面重构为覆盖主应用导航的独立工作台，并按参考页实现紧凑顶栏、固定左右栏、中间播放器与底部时间轴的整体结构。
- [x] 3.5 将待切片视频和白名单效果实现为可分类的两列素材库，支持视频点击/拖放追加、效果点击/拖放应用和当前反馈。
- [x] 3.6 实现受控播放器操作条：播放/暂停、逐帧、静音、片段进度定位、全屏以及跨片段播放状态联动。
- [x] 3.7 实现带工具栏、图例、动态刻度、缩放、横向滚动、点击定位、插入位置拖拽重排、播放头和删除操作的单轨时间轴。
- [x] 3.8 完善右侧片段名称、类型、工程起始、源范围、时长、音量和效果属性，以及导出进度、取消、失败重试和成功状态。
- [x] 3.9 更新浏览器演示 API，使本地工程和导出命令明确不可用且不伪造媒体操作。

## 4. 验证与交付

- [x] 4.1 增加 Rust repository、workflow、command 与导出服务测试，覆盖迁移、选择、输入授权、FFmpeg 参数、取消和临时文件清理。
- [x] 4.2 增加前端测试，覆盖阈值校验、自动选择、无上限分页、进入/返回编辑器、素材应用、播放器控制、时间轴缩放/定位/拖拽、属性保存和导出状态。
- [x] 4.3 运行 Rust 核心/Tauri 测试、前端全量测试、生产构建、格式、Clippy 和 `openspec validate --strict`，修复所有与本变更相关的失败。

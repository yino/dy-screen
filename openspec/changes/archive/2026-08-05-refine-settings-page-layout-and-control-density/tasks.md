## 1. 建立设置页回归基线

- [x] 1.1 检查 `git status` 和活动 OpenSpec 变更，记录 `add-synced-ai-transition-materials` 对 `App.tsx`、`styles.css` 或测试的重叠修改，并保证实施过程中不覆盖其产物
- [x] 1.2 扩展 `App.test.tsx` 形成失败测试，覆盖左列录像/桌面/抖音访问、右列环境/授权/Provider 的分组与稳定 DOM 顺序
- [x] 1.3 增加普通设置脏状态失败测试，覆盖初始未修改、修改后可保存、保存中防重复、成功重置、失败保留输入和外部设置刷新不覆盖未保存输入
- [x] 1.4 增加 Provider 隔离失败测试，确认保存 Provider、测试连接和清除 Key 不提交普通设置，也不改变普通设置脏状态
- [x] 1.5 建立视觉检查页或可重复浏览器状态，覆盖长授权消息、长访问原因、Provider 反馈、资源失败、亮色与深色主题

## 2. 重构设置页结构

- [x] 2.1 将 `SettingsPage` 卡片重组为两个显式 `.settings-column` 容器，按已确认顺序分配六个分组，并保持外层普通设置表单与所有现有字段、事件和可访问名称
- [x] 2.2 为设置主体增加约 1440px 的最大内容宽度、居中约束和独立列间距，使左右卡片只在本列自然堆叠且不再共享隐式网格行高度
- [x] 2.3 实现可用宽度不足时的单列重排，在 1024x768 下保持稳定阅读/键盘顺序且不产生页面级横向滚动
- [x] 2.4 调整卡片标题、内容内边距、表单网格、开关行、状态摘要和说明区域，使长文本自然换行并推动后续内容

## 3. 统一文字和控件尺寸

- [x] 3.1 增加语义化 CSS 尺寸变量，落实设置页正文 13px、辅助文字 11-12px、标签 12px、卡片标题 17px、输入/选择器 44px、普通按钮 40px、紧凑按钮 36px 和稳定图标按钮尺寸
- [x] 3.2 审计并移除共享界面的正负 `letter-spacing`，将中文、品牌、页面标题、栏目提示、表头和按钮文字统一为 0，并使用字重、颜色、行高和间距保留层级
- [x] 3.3 统一主按钮、次按钮、幽灵按钮和危险按钮的图标、内边距、焦点、悬停、禁用与加载状态，确保文字或状态变化不引起布局跳动
- [x] 3.4 将重新激活、访问会话和 Provider 操作改为可换行的内容宽度操作组，宽屏不强制铺满，窄屏按需切换为全宽
- [x] 3.5 同步浅色与深色主题下的新文字、表单、边框、危险状态和吸附操作栏样式，保证对比度和状态含义一致

## 4. 完善普通设置保存反馈

- [x] 4.1 以当前表单和最近加载/成功保存快照计算普通设置 dirty，并在 pristine 时保持保存栏低强调且禁用重复保存
- [x] 4.2 让普通设置提交暴露可等待结果，表达 dirty、saving、saved 和 error 状态；保存失败保留本地输入并允许重试，外部刷新不得静默覆盖 dirty 表单
- [x] 4.3 压缩吸附保存栏的高度、圆角和阴影，增加底部滚动留白，并用 `aria-live` 或等价机制发布保存结果而不遮挡最后一张卡片
- [x] 4.4 保持 Provider 的凭据保存、连接测试、Key 清除和反馈完全独立，验证普通设置提交载荷与现有后端契约不变

## 5. 自动化与视觉验收

- [x] 5.1 运行设置页与全量前端 Vitest，确认分组、字段、访问/授权命令、Provider 隔离和普通设置保存状态全部通过
- [x] 5.2 运行 TypeScript 类型检查和生产构建，确认异步保存契约、样式类和现有页面编译无回归
- [x] 5.3 在 1920x1080、1440x900、1280x800、1024x768 下生成亮色设置页截图并检查元素边界，确认双列/单列断点、内容宽度、卡片间空白、按钮/input 尺寸、保存栏遮挡和横向溢出符合规格
- [x] 5.4 在深色主题、长错误/说明以及 125% 和 150% 字体或页面缩放下重复视觉检查，确认文字换行、控件增长、主要操作和焦点状态无重叠或截断
- [x] 5.5 抽查监控中心、视频库和 AI 页面中受共享按钮及零字间距影响的控件，确保布局结构、固定尺寸工具栏和专业高密度工作区没有回归
- [x] 5.6 运行 `openspec validate --all --strict`、`git diff --check` 和 `git status --short`，确认本变更完整且未覆盖其他活动变更

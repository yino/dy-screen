## 1. 资源协议与清单

- [x] 1.1 定义 Runtime Resource Pack manifest v2 的 Rust 类型，覆盖 schema、应用最低版本、平台架构、组件、归档、许可证和下载地址。
- [x] 1.2 实现 manifest 的 Ed25519 签名验证、公钥内嵌、版本比较和 HTTPS 地址校验。
- [x] 1.3 扩展 `asr-bundle` staging，输出媒体、ASR、模型、规范化字典、许可证和 Windows 运行库的单平台资源目录。
- [x] 1.4 增加资源包 fixture、固定大小/哈希清单和跨平台资源缺失测试。

## 2. SQLite 状态与原子安装

- [x] 2.1 增加 `runtime_resources` 和 `runtime_downloads` SQLite migration，支持旧数据库空状态补全和重复迁移。
- [x] 2.2 实现资源状态、组件状态、进度、错误代码、manifest 哈希和当前版本 repository。
- [x] 2.3 实现 `.part` 临时目录、单资源写锁、fsync、原子 marker 和 `current` 版本指针。
- [x] 2.4 实现启动清理遗留临时目录、保留一个旧版本和失败安装回滚。
- [x] 2.5 增加资源安装事务测试，覆盖归档哈希错误、文件哈希错误、越界路径、符号链接和磁盘不足。

## 3. HTTPS 下载器

- [x] 3.1 实现固定基础地址的 manifest/index 获取和签名校验，不接受 WebView 运行时 URL。
- [x] 3.2 实现带 HTTP Range、ETag/Last-Modified、超时、大小上限和取消 token 的资源下载器。
- [x] 3.3 实现断点恢复、服务器不支持 Range 时重新下载、单资源锁和重复启动订阅。
- [x] 3.4 发布下载阶段、组件、字节进度、校验阶段和失败原因的 Tauri events。
- [ ] 3.5 增加 fake HTTP server 测试，覆盖断网恢复、取消、重试、错误状态持久化和不上传业务数据。

## 4. 启动资源门禁

- [ ] 4.1 将应用启动拆分为资源检查阶段和主功能初始化阶段，资源未就绪时不得构造主播 supervisor、录制器或 ASR scheduler。
- [x] 4.2 让启动流程优先解析已验证的 app data 资源，其次解析安装包内资源，并拒绝未发布目录。
- [x] 4.3 实现资源 ready、downloading、verifying、failed、cancelled、unsupported 和 rollback 状态恢复。
- [x] 4.4 确保资源准备期间退出不会创建活动录制，完成后继续现有数据库迁移、主播恢复和 ASR recovery。
- [x] 4.5 增加启动门禁测试，证明资源缺失时主功能命令和后台任务均不可执行。

## 5. 媒体与 ASR runtime 接入

- [x] 5.1 抽取统一 Runtime Resource Resolver，返回经过校验的 FFmpeg、FFprobe、Whisper、VAD、模型和库路径。
- [x] 5.2 修改录制、视频预览、首帧封面和 ASR 装配，统一使用 resolver 路径并保留原始错误分类。
- [x] 5.3 保留旧 `ffmpeg_path`/`ffprobe_path` 数据迁移字段，但生产命令不得使用用户或前端提交的任意可执行路径。
- [x] 5.4 将本地 ASR 诊断扩展为资源包状态、版本、签名、哈希、权限、内存和磁盘的组件级结果。
- [ ] 5.5 增加资源就绪、资源损坏、Windows 运行库缺失和不支持架构的运行时测试。

## 6. Tauri commands 与前端资源准备页

- [x] 6.1 增加资源状态、资源清单、下载确认、取消、重试、重新检测和清理旧版本的类型化 commands。
- [x] 6.2 增加资源状态 DTO 和事件订阅，前端不得收到完整本地路径、认证头或可执行命令行。
- [x] 6.3 实现首次启动资源准备页，展示版本、平台、组件、总大小、磁盘要求和固定服务器来源。
- [x] 6.4 实现下载进度、校验进度、取消、失败原因、重试、重新检测和退出应用交互。
- [x] 6.5 在资源未 ready 时锁定监控、录制、视频库和 AI 剪辑导航，避免页面直接调用业务命令。
- [ ] 6.6 增加前端空状态、长错误、断网、重启恢复、键盘操作和可访问性测试。

## 7. 发行构建与资源托管输出

- [x] 7.1 更新 macOS/Windows Tauri bundle 配置，将平台资源映射到受控安装目录并保留许可证。
- [x] 7.2 更新 Makefile，增加资源包构建、签名、归档/分片、资源发布目录、基础 URL 注入和安装包验收目标。
- [x] 7.3 让 `make app-build` 明确区分普通开发构建和强制资源发行构建，发行构建缺资源时必须失败。
- [x] 7.4 生成适配自有 HTTPS 服务器的 channel/appVersion/platform/arch/bundleVersion 目录和 index/manifest 文件。
- [x] 7.5 固定第三方组件、模型、FFmpeg、VC++ 运行库的许可证和来源清单，不将本机绝对路径写入产物。
- [x] 7.6 在用户提供资源服务器地址后配置发行 channel，并用静态文件服务验证 Range、缓存和大文件下载。

## 8. 安全与隐私边界

- [x] 8.1 拒绝归档绝对路径、`..`、符号链接、未声明文件和不匹配平台的可执行文件。
- [x] 8.2 对下载 URL、请求头、本地用户目录、临时路径和资源错误执行日志脱敏。
- [x] 8.3 增加资源公钥固定、manifest 签名失败、哈希篡改和降级版本攻击测试。
- [x] 8.4 验证资源下载器不会上传视频、音频、转写、主播信息或用户数据库内容。
- [x] 8.5 确认生产设置和前端 API 不提供任意可执行路径、任意资源 URL 或校验绕过开关。

## 9. 集成与发行验收

- [x] 9.1 运行 Rust 格式、Clippy、资源协议、SQLite、下载器、启动门禁和 runtime 全量测试。
- [x] 9.2 运行 React 类型检查、构建、资源准备页交互和可访问性测试。
- [ ] 9.3 在断网环境验证完整 macOS arm64/Windows x64 安装包可以启动并执行录制、预览、封面和本地 ASR。
- [ ] 9.4 在缺失资源环境验证应用只进入资源准备页，下载完成后无需重新安装即可解锁主功能。
- [ ] 9.5 在篡改、断点、取消、重试、磁盘不足、版本回滚和重复启动场景验证不产生半成品资源。
- [ ] 9.6 在 macOS arm64 和 Windows x64 目标机验证 Unicode 路径、权限、动态库、VC++ 运行库和 sidecar 架构。
- [x] 9.7 运行现有多主播录制、视频库、预览、封面、ASR、AI 剪辑和数据库迁移回归测试。

## 10. 文档与交付

- [x] 10.1 更新 README，说明普通构建、强制资源构建、资源包组成、用户下载流程和自有服务器目录约定。
- [x] 10.2 增加用户可读的资源准备错误码、磁盘/网络要求、下载取消和修复说明。
- [x] 10.3 增加发行清单，记录应用版本、资源版本、平台架构、组件哈希、许可证、签名和验收证据。
- [ ] 10.4 在用户提供最终资源地址后完成 manifest URL、签名公钥指纹和端到端下载验收配置。
- [x] 10.5 运行 `openspec validate --all --strict`、敏感信息审计和 `git diff --check`，确认所有规格与任务状态一致。

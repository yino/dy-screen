## 1. ASR 契约与跨平台资源基线

- [x] 1.1 固定短视频、无人声、纯音乐、中英混合、连续分片和 Windows 中文路径 fixture，并记录预期音轨、时长及转写时间边界。
- [x] 1.2 定义与具体模型无关的 `AsrRequest`、`AsrResult`、`AsrCapabilities`、进度事件和统一错误分类，明确可选置信度与不支持能力的行为。
- [x] 1.3 选择并锁定首版 `whisper.cpp` 版本、small 多语言量化模型格式、VAD 模型版本、来源、许可证、文件大小和 SHA-256。
- [x] 1.4 定义随包资源 manifest，包含 sidecar 平台/架构、引擎版本、模型逻辑 ID、模型版本、大小、哈希和最低资源要求。
- [x] 1.5 在 macOS arm64 目标机验证启用 Metal 的 `whisper.cpp` CLI、结构化输出、取消、模型加载和签名后执行行为。
- [ ] 1.6 在 Windows x64 目标机验证 CPU `whisper.cpp` CLI、Unicode/空格路径、结构化输出、取消、最低 CPU 指令和 VC++ 运行库要求；使用 `make asr-test-windows-target` 生成脱敏、不可覆盖的目标机证据。

## 2. AI 领域模型与 SQLite 持久化

- [x] 2.1 先编写数据库迁移测试，覆盖旧数据库升级、重复迁移、外键完整性和现有录像数据不变。
- [x] 2.2 新增 `ai_projects`、`ai_project_inputs`、`asr_artifacts` 和 `transcript_segments` 表、索引及约束，确保句段具有可稳定引用的 ID。
- [x] 2.3 定义项目、输入、识别配置、引擎/模型身份、产物、稳定句段、源内/项目时间范围、阶段状态和脱敏错误的 Rust 领域类型。
- [x] 2.4 先编写 repository 测试，覆盖草稿 CRUD、有序输入、状态转换、事务发布、部分完成和重启恢复查询。
- [x] 2.5 实现 AI 项目与输入 repository，并拒绝不合法的状态回退或运行中输入修改。
- [x] 2.6 实现 ASR 产物按源指纹、引擎身份和识别配置指纹查找、事务发布、引用及最后使用时间更新。
- [x] 2.7 实现项目删除、录像删除后的产物失效及无引用产物清理，并验证不会删除原始媒体或随包模型。

## 3. 多视频项目服务

- [x] 3.1 先编写项目服务测试，证明打开 AI 页面、录像完成和应用启动均不会自动创建项目或 ASR 任务。
- [x] 3.2 实现创建草稿、重命名、列出、查看、删除和输入冻结操作。
- [x] 3.3 先编写本地导入测试，覆盖多选、顺序、重复源、无效路径、源变化和不复制原视频。
- [x] 3.4 实现受信本地文件输入解析、规范路径验证、源指纹生成和重复项去除。
- [x] 3.5 先编写直播会话选择测试，覆盖已结束会话按时间展开、进行中会话拒绝、缺失分片标记和视频库 ID 保留。
- [x] 3.6 实现会话到完成分片输入的解析，并复用现有录像会话与视频库查询。
- [x] 3.7 实现草稿输入重排、移除、音轨/时长摘要以及引擎、模型和环境状态摘要。
- [x] 3.8 实现开始分析前的统一 preflight；sidecar、模型、内存或磁盘检查失败时保持草稿且不创建运行任务。

## 4. 音频准备与 VAD

- [x] 4.1 定义可注入的 `MediaAudioPreparer` 和 `VadEngine` trait，并用假执行器覆盖成功、无音轨、无人声、失败和取消测试。
- [x] 4.2 先编写 FFprobe 探测测试，覆盖有效音轨、无音轨、未知时长、仍在写入文件和源指纹变化。
- [x] 4.3 实现只读取原始视频的 FFprobe 输入验证，不依赖或生成预览 MP4。
- [x] 4.4 先编写 FFmpeg 计划与临时目录测试，验证 16 kHz 单声道 16-bit PCM、参数安全、磁盘错误和原视频哈希不变。
- [x] 4.5 实现 PCM 管道能力和项目隔离临时 WAV 回退，并在成功、失败、取消和启动恢复时清理临时文件。
- [x] 4.6 实现 VAD 适配器、约 500 毫秒可配置边界保护和源时间戳映射，并测试静音、短停顿、纯音乐及首尾音节场景。

## 5. AsrEngine 与 WhisperCppEngine

- [x] 5.1 先使用假 `AsrEngine` 编写契约测试，验证项目、调度、缓存和时间轴代码不依赖 Whisper 命令行或专属输出字段。
- [x] 5.2 实现 `AsrEngine` trait 的能力查询、环境校验、异步识别、进度回调和取消边界。
- [x] 5.3 实现 macOS arm64 与 Windows x64 应用资源解析器，自动定位当前平台 sidecar、模型、VAD 和 manifest，并拒绝不匹配架构。
- [x] 5.4 实现随包资源校验，覆盖文件存在性、大小、SHA-256、版本兼容、可执行权限和模型逻辑 ID。
- [x] 5.5 实现 8 GB 基线 preflight，检查当前可用内存、模型预计占用、线程数和磁盘安全余量。
- [x] 5.6 实现 Rust `WhisperCppEngine`，使用参数数组启动 `whisper.cpp` CLI 并保证生产环境不能使用任意 sidecar 路径。
- [x] 5.7 实现 macOS arm64 Metal 启动、退出、取消和错误映射测试。
- [x] 5.8 实现 Windows x64 CPU 启动、Unicode/空格路径、退出、取消、运行库缺失和错误映射测试。
- [x] 5.9 实现结构化输出解析、句段时间戳、语言、引擎/模型版本、警告和可选置信信息，缺少置信度时保存为空。
- [x] 5.10 实现原始文本到简体中文、标点、数字和金额规范化的带版本后处理，并保持原始文本不可变。
- [x] 5.11 确保每个输入完成、失败或取消后退出 sidecar、释放模型内存和文件句柄，不实现常驻 Provider 进程。

## 6. 逻辑时间映射、产物复用与任务调度

- [x] 6.1 先编写逻辑时间映射测试，覆盖稳定句段 ID、多输入累计偏移、毫秒精度、失败输入空洞、外部视频无上下文共享和连续直播分片有界去重；该映射不涉及图形化剪辑时间轴。
- [x] 6.2 实现 `TranscriptAssembler`，为每个项目结果提供稳定句段 ID、视频 ID、源视频时间范围和项目时间范围，且不物理拼接媒体。
- [x] 6.3 先编写缓存测试，覆盖完全命中、源变化、引擎/模型/VAD/热词变化、半成品拒绝和跨项目复用。
- [x] 6.4 实现识别配置指纹与原子产物发布，让缓存命中跳过 FFmpeg、VAD 和 ASR。
- [x] 6.5 先编写调度器测试，覆盖全局单并发、FIFO 排队、录制活动门控、窗口隐藏、取消和许可释放。
- [x] 6.6 实现独立 ASR 调度器和录制优先执行门，不占用现有录制并发许可，并限制 `whisper.cpp` 线程数。
- [x] 6.7 实现项目及输入进度聚合、`completed_with_errors` 判定和可恢复状态事件。
- [x] 6.8 实现显式退出与异常重启恢复，终止遗留子进程、清理半成品并把未完成输入恢复为可重试状态。

## 7. Tauri 命令与安全边界

- [x] 7.1 为项目 CRUD、本地输入、会话选择、输入排序、开始、取消、重试、删除、状态查询、转写查询、复制投影和 TXT/JSON 导出定义类型化 Tauri command。
- [x] 7.2 使用系统文件选择能力建立受信本地输入入口，并测试前端不能用任意路径直接启动媒体或模型进程。
- [x] 7.3 提供本地 ASR 环境诊断 command，返回平台、sidecar、模型、VAD、manifest、完整性、内存和磁盘的中文状态。
- [x] 7.4 保持生产设置不包含 `whisper.cpp`、ASR 模型或 VAD 模型任意路径；只为开发测试提供显式且隔离的资源覆盖方式。
- [x] 7.5 接入 AI 状态事件与主动查询恢复，确保窗口隐藏、页面重建和事件丢失后结果一致。
- [x] 7.6 扩展应用 shutdown 和启动清理，覆盖 ASR 子进程、临时音频、运行中数据库状态与无引用产物。
- [x] 7.7 审计 command 参数、事件、普通日志和错误映射，验证不暴露签名 URL、未授权本地路径或原始模型 stderr。

## 8. AI 剪辑工作区界面

- [x] 8.1 先更新前端 API 与组件测试，覆盖无项目空状态、环境不可用、创建草稿、本地多选和会话选择。
- [x] 8.2 将 AI 规划中占位页替换为项目列表和项目详情工作区，保留紧凑桌面布局及现有导航行为。
- [x] 8.3 实现本地视频导入和已结束直播会话选择界面，明确显示所有识别均在本机完成。
- [x] 8.4 实现输入列表的来源、音轨、时长、错误、拖动排序、移除、视频数量和总时长摘要。
- [x] 8.5 展示 `WhisperCppEngine`、small 模型版本、平台支持、内存/磁盘检查和环境诊断结果，诊断失败时禁用开始分析。
- [x] 8.6 实现重新检测和安装修复说明，不提供模型下载、离线导入、多模型切换或任意路径配置控件。
- [x] 8.7 实现排队、等待录制资源、探测、提取音频、VAD、识别、合并、完成、部分完成、取消和失败状态。
- [x] 8.8 实现取消、失败项重试和项目删除确认，清楚区分删除 AI 数据与删除原始视频。
- [x] 8.9 实现左侧有序视频及状态、主区域当前视频播放器和只读时间戳文本列表；第一版不实现波形、多轨道、缩放、拖动或裁剪组成的图形化时间轴。
- [x] 8.10 复用现有预览能力实现内联播放器，并保证预览只服务界面播放、缺失或清理预览不会影响原始视频 ASR 与产物复用。
- [x] 8.11 实现点击句段跳转到源视频时间、播放时高亮当前句段，以及可关闭的“跟随播放”；无法可靠映射预览时间时禁用联动但保留文本浏览。
- [x] 8.12 实现仅存在于 WebView 的“显示字幕”覆盖，切换视频或离开页面时清理，并验证不会生成字幕文件、修改视频、写入预览缓存或调用 FFmpeg 渲染。
- [x] 8.13 实现单句、当前视频全文和项目全文复制，以及包含视频边界和可读时间戳的 TXT 导出。
- [x] 8.14 实现 JSON 导出，包含项目、输入、稳定句段 ID、视频 ID、源内/项目时间范围、原始/规范化文本及可选置信信息。
- [x] 8.15 对长直播转写列表使用分页或虚拟化，只在当前句段变化时更新高亮，并测试关闭跟随后自由阅读。
- [x] 8.16 覆盖宽屏左右布局、窄窗口上下布局、长文件名、长句段、长错误文本、Windows 路径、键盘操作和无障碍测试。
- [x] 8.17 验证第一版工作区不提供转写编辑、句段拆分/合并、SRT/ASS、字幕样式、波形、多轨道、裁剪或视频渲染导出入口。

## 9. 平台打包、集成验证与文档

- [x] 9.1 配置 macOS arm64 安装包资源，包含签名的主程序、Metal `whisper.cpp`、FFmpeg/FFprobe、small 模型、VAD 和 manifest。
- [ ] 9.2 配置 Windows x64 安装包资源，包含签名的主程序、CPU `whisper.cpp`、FFmpeg/FFprobe、small 模型、VAD、manifest 和所需运行库；由 `make asr-verify-release-windows` 核对安装后全部二进制签名、时间戳、资源哈希和运行库。
- [x] 9.3 生成第三方组件与模型许可证清单，并验证模型、FFmpeg、`whisper.cpp` 的分发条件。
- [ ] 9.4 在 macOS 完成 sidecar 签名、notarization、应用资源定位和安装后离线运行测试；使用 `make asr-verify-release-macos` 生成 Developer ID、stapling、Gatekeeper、DMG、包内资源、离线 ASR 和启动证据。
- [ ] 9.5 在 Windows 完成代码签名、SmartScreen、安装/卸载、VC++ 运行库、中文用户目录和安装后离线运行测试；使用 `make asr-verify-release-windows` 生成 Authenticode、时间戳、SmartScreen 证据引用、运行库、离线 ASR 和卸载证据。
- [x] 9.6 添加端到端后端测试，覆盖外部多视频项目、整场直播项目、部分失败、取消、重启恢复、缓存复用、稳定句段 ID 以及 TXT/JSON 导出投影。
- [x] 9.7 添加真实 FFmpeg/FFprobe/Whisper fixture 测试，验证无音轨、无人声、VAD 时间映射、中文转写、临时文件清理和原始文件哈希不变。
- [ ] 9.8 在 macOS arm64 与 Windows x64 的 8 GB 目标设备上记录处理实时因子、峰值内存、线程数、温度/稳定性和资源释放；分别使用 `make asr-performance-macos` 与 `make asr-performance-windows` 的严格 8 GB 门禁。
- [ ] 9.9 使用至少 10 场真实中文直播样本记录商品名、金额、主播名召回率、时间误差、静音幻觉率和失败率；使用 `asr-quality collect/evaluate` 封存授权引用、数据集/输入/结果哈希并生成 JSON/Markdown 报告。
- [x] 9.10 验证存在活动录制时 ASR 等待且录制输出、分片登记、磁盘保护和预览行为无回归。
- [x] 9.11 运行 Rust、Tauri 和 React 全量测试、格式化及静态检查，并修复所有回归。
- [x] 9.12 更新 README，说明 AI 项目流程、播放器与时间戳文本联动、临时字幕覆盖、TXT/JSON 导出、随包模型、8 GB 基线、macOS/Windows 支持、本地资源消耗、安装修复和当前非目标。
- [x] 9.13 对照三份 delta spec 逐项验收；若 small 模型中文指标不达标，记录结论并建议另立用户授权的云端 API Adapter 变更，不在本变更实现自动回退。

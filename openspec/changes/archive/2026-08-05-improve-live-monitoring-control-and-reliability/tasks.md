## 1. 建立失败测试与契约基线

- [x] 1.1 扩展 `client_api_contract` 和 API 单元测试，覆盖 AppStart/心跳精确解析可选 `max_screen_limit`、两个接口仅一方携带字段、字段缺失、非法值和请求失败保持当前额度
- [x] 1.2 增加数据库 migration/repository 失败测试，覆盖旧库按主播 ID 回填优先级、新主播追加、原子上移/下移、归档恢复、身份合并、幂等执行、记录数和外键保持不变
- [x] 1.3 增加 Supervisor 普通系统线程回归测试，在没有当前 Tokio reactor 时调用单个/全部启动入口，复现并锁定不再发生 `there is no reactor running` panic
- [x] 1.4 增加优先级调度失败测试，覆盖 6 个在线主播额度 4、`a,b,c,d` 切换为 `a,b,c,e`、离线不占位、空位自动补入和等待期间持续复查
- [x] 1.5 增加动态额度失败测试，覆盖 4 提升至 6 的补位、4 降至 3 的最低优先级抢占、先释放许可后启动替代任务以及本地旧设置不能改变额度
- [x] 1.6 增加验证提示协调器测试，覆盖首次弹窗、重复状态去重、稍后处理、点击立即验证、恢复后重置和关闭期间不启动验证任务
- [x] 1.7 增加前端失败测试，覆盖录制优先级显示/调整/失败回滚、等待资源状态、设置页只读服务端额度以及不再出现本地并发输入框

## 2. 持久化录制优先级

- [x] 2.1 新增幂等 SQLite migration 和 `recording_priority` 领域字段，按现有主播 ID 升序回填并保留全部主播、会话、视频、监听状态和外键
- [x] 2.2 实现 repository 的稳定优先级查询和原子上移/下移操作，以主播 ID 处理重复序号兜底，并在写入失败时保持原顺序
- [x] 2.3 在主播创建、归档恢复、来源更新和重复身份合并路径中维护优先级，新主播追加末尾且合并保留更高优先级
- [x] 2.4 新增类型化 Tauri 优先级 command、MonitorEvent 和 Dashboard/Streamer DTO 字段，并保证 command 成功后触发目标集合重算和界面刷新

## 3. 接入服务端录制额度

- [x] 3.1 在 AppStart 与心跳响应 DTO 中用显式 serde rename 增加可选 snake_case `max_screen_limit`，只接受可转换为 `usize` 的正整数并对非法值生成脱敏诊断
- [x] 3.2 实现默认值为 4 的共享运行时额度状态，使任一接口最新合法值生效，缺失字段或网络失败保留最近合法值
- [x] 3.3 将 AppStart 和心跳生命周期的额度更新连接到 Supervisor，在运行中变化时立即串行触发录制目标重算并发布只读额度状态
- [x] 3.4 停止从 `AppSettings.max_concurrent_recordings` 初始化或更新 Supervisor；保留旧 SQLite key/载荷兼容但忽略其调度影响

## 4. 修复 worker 运行时闪退

- [x] 4.1 为 Supervisor 引入可测试的应用级任务调度器，生产环境使用 Tauri 全局异步运行时，测试环境使用 Tokio handle 或可控 fake
- [x] 4.2 重构 `start` 的去重、generation 和登记顺序，使调度失败可返回错误并撤销占位，不留下虚假 worker 或修改用户监听选择
- [x] 4.3 让立即检查、恢复监听、访问恢复、系统唤醒、主播合并和启动恢复全部复用安全启动入口，并消除调用方对当前 Tokio reactor 的隐含依赖
- [x] 4.4 运行普通线程、同步 command、并发启动、worker generation 和应用生命周期测试，确认无 panic、无重复 worker 且关闭仍可有界等待

## 5. 实现优先级录制协调器

- [x] 5.1 在 Supervisor 中建立串行化的在线候选、等待候选、活动录制、generation 和当前额度状态，避免在同步锁内执行异步等待
- [x] 5.2 将开播 worker 从直接竞争信号量改为登记候选并等待目标分配，等待期间按既有周期复查在线状态，获配后重新解析最新地址
- [x] 5.3 实现确定性 reconcile，按 `monitor_enabled`、在线状态、`recording_priority` 和主播 ID 计算前 N 个目标，并在优先级/在线状态/额度/录制退出变化时触发
- [x] 5.4 实现优先级切换和服务端降额的协同抢占，先优雅取消被移出录制并等待 permit 释放，再按顺序启动新目标，确保瞬时活动数不超额
- [x] 5.5 为 `priority_changed`、`server_limit_reduced` 和普通用户停止区分稳定取消原因，关闭会话、对账完成分片并写入脱敏诊断
- [x] 5.6 覆盖暂停/恢复、归档、身份合并、访问验证、磁盘保护、录制重试和应用退出对候选与活动状态的清理，防止旧 generation 回写

## 6. 增强验证码系统提醒

- [x] 6.1 把现有通知去重布尔值重构为共享验证提示协调器，按一次 `verification_required` 状态周期管理显示、延后和恢复重置
- [x] 6.2 使用 `tauri-plugin-dialog` 非阻塞 warning 对话框显示“立即验证”和“稍后处理”，使关键弹窗不受普通通知开关影响且后台状态仍可见
- [x] 6.3 将“立即验证”回调连接到当前 `RoomResolutionService.show_verification`，通过 Tauri 全局运行时显示并聚焦持久 WebView；稍后处理只保留横幅
- [x] 6.4 在回调中检查 AppState、关闭门和当前访问状态，防止退出期间启动任务、恢复后打开过期窗口或多主播重复堆叠弹窗
- [x] 6.5 保留普通系统通知的用户开关、主界面持续横幅、浏览器演示限制和验证窗口导航白名单，并运行访问会话回归测试

## 7. 更新桌面界面与兼容层

- [x] 7.1 同步 Rust/TypeScript 的主播优先级和只读 `maxScreenLimit` 类型，扩展 Tauri API 与浏览器演示实现但不伪造服务端调额或系统弹窗
- [x] 7.2 在监控表格显示独立优先级序号和可访问的提高/降低图标操作，提供工具提示、边界禁用、提交中状态、成功刷新和失败回滚
- [x] 7.3 保持现有状态排序不改变优先级，在直播数超额时清楚区分录制中与等待资源，并验证窄屏和桌面布局无文字或控件重叠
- [x] 7.4 从设置页移除最大并发数字输入，改为只读展示“服务端录制额度 N 路”，保存其他设置时不提交或应用本地额度
- [x] 7.5 更新 README 和相关中文 wiki，说明服务端额度、录制优先级、验证码强提醒及正确的 `DEV_REQUIRE_ACTIVATION=1` 开发命令

## 8. 验证与交付

- [x] 8.1 运行 `cargo fmt --all --check`、两个 Rust workspace 的目标测试与 Clippy，修复所有新增告警且不跳过失败断言
- [x] 8.2 运行前端 Vitest、TypeScript 类型检查和生产构建，确认优先级交互、只读额度及现有监控/设置流程通过
- [x] 8.3 运行 `make test-supervisor-profile`、`make test-browser-access`、`make test-app-lifecycle` 和相关数据库/API 回归，验证调度、访问和退出边界
- [ ] 8.4 在 macOS 桌面实机验证 6 个在线候选中从 `a,b,c,d` 切换到 `a,b,c,e`、服务端动态升降额度、系统唤醒无闪退及验证码弹窗一键验证，并保存脱敏验收记录
- [x] 8.5 运行 `openspec validate --all --strict`、`git diff --check` 和 `git status --short`，确认本变更完整且未覆盖其他活动变更

## 1. 数据契约与迁移

- [x] 1.1 为 DeepSeek 设置增加默认 8 分的 `transitionAutoApplyScore` 字段，补齐 SQLite 向前迁移、读写校验和前后端 DTO
- [x] 1.2 为转场匹配运行和边界结果增加冻结阈值、两阶段状态、0–10 总分、评分维度及脱敏审计持久化
- [x] 1.3 为工程级文本纠错定义运行结果、进度、统计和错误 DTO，并增加必要的脱敏运行审计存储

## 2. 一键文本纠错

- [x] 2.1 在 Rust 中按稳定顺序读取未隐藏且未经人工修改的工程字幕，并实现完整字幕边界上的有界分批
- [x] 2.2 扩展 LLM Provider 的保守文本纠错请求、结构化响应和长度、索引、文本合法性校验
- [x] 2.3 实现冻结工程版本、全部批次成功后单事务提交、冲突回滚和导出状态校验
- [x] 2.4 增加一键文本纠错 Tauri command、进度事件和取消路径，并注册到桌面应用

## 3. 两阶段转场 Agent

- [x] 3.1 将现有匹配契约改为每边界最多三个候选或 `none`，并严格限制在本地确定性召回集合内
- [x] 3.2 增加独立评分 Agent 契约，校验候选集合、0–10 总分、四个评分维度和理由
- [x] 3.3 编排匹配、评分、确定性最佳候选和冻结阈值应用，保留人工锁并保存低分与无转场建议
- [x] 3.4 增加阶段进度、运行统计、脱敏审计和取消检查，确保迟到响应不会继续应用

## 4. 前端交互

- [x] 4.1 在设置页增加 0–10 整数转场自动应用阈值控件并接入现有 DeepSeek 设置保存
- [x] 4.2 将“一键文本纠错”接入真实 command，展示禁用原因、阶段进度、取消、统计和工程刷新
- [x] 4.3 将“一键 Agent”接入两阶段运行，展示冻结阈值、阶段、取消、结果统计和评分明细

## 5. 验证

- [x] 5.1 补充 Rust 单元与 repository 测试，覆盖默认阈值、结构化校验、人工字幕跳过、批次失败回滚、版本冲突和匹配评分阈值应用
- [x] 5.2 补充前端 API 与交互测试，覆盖按钮门禁、运行进度、取消、成功刷新、失败保持和设置校验
- [x] 5.3 运行 Rust 测试、前端 typecheck/test/build、`git diff --check` 和 OpenSpec strict validate，并修复全部回归
- [x] 5.4 处理新前端连接旧 Tauri 后端的命令缺失回归，补充可操作提示并重建桌面应用验证命令注册
- [x] 5.5 将 Provider 未保存状态纳入底部保存栏，使页面级保存可提交转场阈值，并验证两种保存入口状态一致
- [x] 5.6 修复 DeepSeek 原生结构化调用兼容性，使用响应元数据记录 token 用量并区分 Provider 与结构错误

## 1. 缩略图领域模型与执行计划

- [x] 1.1 在 Tauri 后端定义缩略图状态、批次、资源描述、失败码和事件 DTO，并同步 TypeScript 类型
- [x] 1.2 为原文件和已有预览缓存建立包含视频 ID、规范路径、大小、修改时间、来源类型和配置版本的源指纹与稳定缓存键
- [x] 1.3 先增加 FFmpeg 参数计划测试，覆盖起点首帧、失败后第 1 秒回退、最长边 480px、旋转信息、JPEG 输出和参数数组安全
- [x] 1.4 实现可取消的 FFmpeg 缩略图执行器，验证输出尺寸和文件类型，回收子进程并清理失败临时文件

## 2. 独立缩略图缓存

- [x] 2.1 实现 `app_cache_dir()/video-thumbnails/` 缓存路径、JPEG/sidecar 清单和 `.part.jpg` 原子发布
- [x] 2.2 验证缓存键、缓存根目录、常规文件和清单归属，拒绝目录、符号链接、根目录逃逸及损坏清单
- [x] 2.3 实现相同源版本命中、最后访问时间更新、源版本变化失效和原文件/预览缓存来源切换
- [x] 2.4 实现启动与批次前清理，覆盖临时文件、孤立 JPEG、损坏清单、30 天过期和 512 MB LRU 上限
- [x] 2.5 增加受保护缓存测试，确保生成中或活动批次正在展示的封面不会被清理

## 3. 批次任务与状态服务

- [x] 3.1 实现 `ThumbnailService` 批次创建、视频兴趣引用、同缓存键任务去重和最多 2 个 FFmpeg 的 semaphore
- [x] 3.2 实现批次释放，取消无其他引用且尚未开始的任务，并允许已运行轻量任务完成缓存
- [x] 3.3 实现 `queued`、`ready`、`failed`、`unavailable` 状态存储、事件发布和主动查询恢复
- [x] 3.4 实现同一源版本只自动尝试一次及用户显式重试，确保失败不修改视频业务状态
- [x] 3.5 实现按视频驱逐标记、任务取消和发布前二次校验，防止删除期间运行任务重新创建缓存
- [x] 3.6 实现服务关闭，取消全部缩略图任务、终止并回收 FFmpeg，等待任务退出后完成应用关闭

## 4. Tauri 命令与视频生命周期

- [x] 4.1 在应用启动时初始化缩略图服务、事件发布器和缓存清理，并将其加入受控应用状态
- [x] 4.2 增加当前分页批量请求、批次释放、状态查询和单视频重试 command，只接受数据库视频 ID
- [x] 4.3 实现受信媒体选择：优先已完成原文件，原文件缺失时只读取已有有效 MP4 预览缓存，不触发预览转换
- [x] 4.4 为 `ready` JPEG 执行精确 Tauri asset scope 授权，并增加任意路径、目录、符号链接和缓存根外文件拒绝测试
- [x] 4.5 将单视频删除接入缩略图取消和缓存清理，覆盖删除成功、文件删除失败、数据库失败回滚及发布竞态
- [x] 4.6 将会话删除接入批量缩略图清理，覆盖全部成功和部分失败时缓存保留与业务回滚
- [x] 4.7 将显式退出流程改为等待缩略图服务关闭，并验证不会遗留运行子进程或已发布半成品

## 5. 视频库前端

- [x] 5.1 扩展 `ClientApi` 和浏览器演示 API；演示模式固定返回不可用占位，不读取或伪造本地首帧
- [x] 5.2 在视频库当前分页加载后创建缩略图批次，并在翻页、筛选、离开页面或卸载时释放旧批次
- [x] 5.3 按视频 ID 和当前批次处理事件与主动查询，防止旧分页或旧源版本事件覆盖当前封面
- [x] 5.4 把历史卡片封面改为稳定 16:9 区域，展示居中裁切 JPEG、加载占位、缺失占位和失败重试图标
- [x] 5.5 让可用封面点击调用既有视频预览，保持预览、系统打开、定位和删除按钮行为不变
- [x] 5.6 保持本次监听视频列表不请求缩略图，并验证封面加载和错误文案不会引起卡片尺寸变化或文本重叠

## 6. 回归、实机样本与文档

- [x] 6.1 增加 Rust 缓存、批次去重、并发上限、翻页取消、显式重试、驱逐竞态和关闭等待测试
- [x] 6.2 使用受控横屏、竖屏、无音轨、起点失败、损坏及文件缺失样本验证首帧 JPEG 和第 1 秒回退，不保存用户真实媒体
- [x] 6.3 增加 Tauri 集成测试，覆盖分页批次、已有预览缓存回退、精确资源授权、单视频删除和会话删除
- [x] 6.4 增加 React 测试，覆盖缓存命中、渐进加载、失败重试、点击封面预览、分页释放和浏览器占位
- [x] 6.5 在 Makefile 增加聚焦缩略图单元/真实 FFmpeg 验收目标，并把相关目标纳入现有检查流程
- [x] 6.6 更新 README 的视频库封面、缓存位置、30 天/512 MB 清理规则、FFmpeg 依赖和失败降级说明
- [x] 6.7 运行格式检查、Clippy、前端测试、Rust 核心与 Tauri 测试、真实 FFmpeg 缩略图验收及 `openspec validate --all --strict`

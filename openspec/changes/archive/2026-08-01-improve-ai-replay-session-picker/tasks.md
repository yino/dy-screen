## 1. 数据查询与分页契约

- [x] 1.1 为已结束会话目录补充幂等 SQLite 复合索引迁移，保持现有会话、视频和 AI 项目数据不变
- [x] 1.2 定义主播目录、回放目录、分页响应和稳定游标 DTO，限制默认每页 20 条且后端最大 50 条，并确保查询结果不包含本地文件路径
- [x] 1.3 实现主播目录聚合查询，支持按名称、标签和 `webRid` 绑定参数搜索，并按 `(latest_ended_at, streamer_id)` 稳定游标分页
- [x] 1.4 实现指定 `streamer_id`、`project_id` 的回放目录聚合查询，支持按本地日期、时间和会话 ID 搜索，并按 `(started_at, session_id)` 稳定游标分页
- [x] 1.5 在目录查询中排除进行中和零分片会话，保留异常结束但有分片的会话，并聚合总分片、总时长、不可用和当前项目已导入数量
- [x] 1.6 对搜索词执行去空白、长度限制、`LIKE` 通配符转义和绑定参数处理，并统一数据库 UTC 时间与界面本地时间搜索语义

## 2. 后端测试与命令接入

- [x] 2.1 增加 repository 测试，覆盖超过 200 场历史回放、稳定分页、搜索特殊字符、跨日本地时间和零分片排除
- [x] 2.2 增加主播状态测试，覆盖正在直播、暂停监听和已归档主播仍可查询历史回放
- [x] 2.3 增加项目导入状态测试，覆盖未导入、部分导入、全部导入、不可用分片和异常结束会话
- [x] 2.4 在 AI service 和 desktop runtime 中接入两个分页查询，避免按主播或会话逐条查询的 N+1 路径
- [x] 2.5 注册 Tauri commands 并补齐 TypeScript API 与 DTO 契约，同时保留 `ai_add_completed_session` 对进行中会话、重复项和陈旧选择的最终校验
- [x] 2.6 增加 service、command 和序列化契约测试，确认分页上限、游标错误、未知主播/项目和无本地路径返回行为

## 3. 可搜索组合框

- [x] 3.1 实现不新增依赖的受控 `SearchableCombobox`，支持搜索输入、选项列表、加载更多、禁用项、空结果、加载和失败重试状态
- [x] 3.2 实现方向键、回车、`Esc`、点击外部关闭和关闭后焦点恢复，并补齐 `combobox`、`listbox`、`aria-activedescendant` 与 option 语义
- [x] 3.3 使用防抖和查询 generation 丢弃搜索词、项目、主播或游标变化后的陈旧响应，并保证分页追加结果不重复或重排
- [x] 3.4 增加组合框组件测试，覆盖鼠标与键盘选择、快速搜索竞态、分页、失败重试、禁用选项和焦点恢复
- [x] 3.5 按现有客户端主题完善组合框样式，确保长文本省略、弹层不越界，并在窄窗口按逻辑顺序纵向排列

## 4. AI 工作区二级联动与刷新

- [x] 4.1 将单一历史直播下拉框替换为主播和回放二级联动组合框，主播未选择时禁用回放查询且不默认选择首个主播
- [x] 4.2 在主播项展示标签、`webRid`、归档/监听/直播状态和可选回放数，在回放项展示本地时间、时长、分片数、不可用数及“已添加 N/M”状态
- [x] 4.3 禁用已全部添加的回放，允许部分添加回放补充缺少分片；成功添加后保留主播、清空回放并刷新项目导入状态
- [x] 4.4 在 AI 页面从非活动变为活动及打开选择器时刷新目录，并在查询失败时保留当前项目输入和已经确认的主播选择
- [x] 4.5 监听 `session_changed` 使目录失效：页面活动时立即刷新，否则在下次激活或打开选择器时刷新
- [x] 4.6 增加工作区回归测试，确认当天有分片直播在页面挂载后结束时无需重启即可出现，同时进行中会话始终不进入回放列表
- [x] 4.7 增加连续添加、部分/全部导入、超过 200 场搜索、归档主播、当前直播主播和异常结束回放的交互测试

## 5. 验收与质量检查

- [ ] 5.1 在桌面与窄窗口视口验证二级选择器搜索、滚动分页、键盘导航、弹层边界和连续添加流程
- [x] 5.2 运行前端格式检查、类型检查和全量测试，并修复本次变更引入的问题
- [x] 5.3 运行 Rust 格式检查、相关测试、全量测试和 Clippy，并确认数据库迁移可重复执行
- [ ] 5.4 构建 Tauri 客户端并手工验证录制结束后的 `session_changed` 会让当天回放自动进入对应主播目录
- [x] 5.5 运行 `openspec validate improve-ai-replay-session-picker --strict` 和 `git diff --check`，确保变更文档与实现验收结果一致

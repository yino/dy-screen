## 1. 客户端版本与 API 契约

- [x] 1.1 将 `package.json`、根 package lock、`src-tauri/Cargo.toml`、对应 Cargo lock 和 `src-tauri/tauri.conf.json` 的桌面客户端版本一致升级为 `0.3.0`，并增加版本一致性检查
- [x] 1.2 在统一 Rust API 客户端中增加可选 camelCase 心跳 DTO `transitionMaterials.catalogVersion/minimumAppVersion`，保持既有心跳字段和授权判定兼容
- [x] 1.3 增加转场目录业务信封及素材 DTO，准确解析 `changed`、`cdnBaseUrl`、版本、描述、标签、渲染模式、校验值和媒体属性
- [x] 1.4 为心跳字段缺失/非法、目录成功/业务错误、`changed=false` 无 materials 和示例全量响应增加 API 契约测试

## 2. 目录校验与 SQLite 模型

- [x] 2.1 新增 `transition_catalog_state`、版本化 `transition_materials` 和 `transition_material_downloads` 的向前 SQLite migration、索引和约束
- [x] 2.2 新增 `ai_clip_transition_boundaries` 和 `ai_transition_match_runs` migration，以左右片段稳定 ID、素材键和版本表达工程边界选择
- [x] 2.3 实现目录规范化与校验器，覆盖唯一键、字段范围、已知 `renderMode`、SHA-256、正式 HTTPS、localhost 开发例外、相对 URL 解析和 CDN 同源绝对 URL
- [x] 2.4 实现目录 repository 的事务化全量发布、`changed=false` 保留、版本倒退拒绝、当前版本切换和历史版本保留
- [x] 2.5 实现下载状态、边界选择、人工锁、失效状态和匹配运行的 repository 读写，并确保工程删除/片段变更的外键行为可恢复
- [x] 2.6 增加 migration 与 repository 测试，覆盖首次同步、幂等重放、事务回滚、新版本替换、目录移除、历史工程固定引用和旧数据库升级

## 3. 心跳发现与独立目录同步

- [x] 3.1 在授权生命周期中把合法素材版本信号发布给单实例协调器，确保重复心跳去重且不阻塞 90 秒心跳循环
- [x] 3.2 实现 `minimumAppVersion` 语义版本门禁：低版本进入 `upgrade_required`，但不撤销授权、不停止业务任务或删除上一目录
- [x] 3.3 复用 `ApiClient` 和 Rust 凭据存储调用 `/v1/transition-materials?clientVersion=<localCatalogVersion>`，附带 `device_id` 与 `activate_code` 且不向 WebView 暴露凭据
- [x] 3.4 实现目录同步协调器的串行执行、去重、有界退避、手动重试和脱敏状态发布，使目录故障与授权、监听、录制、ASR 独立
- [x] 3.5 增加生命周期和同步测试，覆盖相同版本、版本变化、字段缺失、最低版本过高、网络失败、非法目录和并发手动重试

## 4. 素材按需下载与兼容预览

- [x] 4.1 在应用数据目录定义不暴露绝对路径的版本化素材与预览缓存布局，并实现受控 Tauri 查询/预览句柄
- [x] 4.2 实现按素材版本合并并发请求的下载器，包含超时、响应大小限制、临时文件、`sizeBytes`/SHA-256 校验、原子发布、失败清理和显式重试
- [x] 4.3 实现缩略图/封面按需缓存和缺图占位，确保前端不能提交任意 URL 触发下载
- [x] 4.4 使用 FFprobe 实际探测源媒体；对 HEVC 或 WebView 不兼容素材通过受控 FFmpeg 生成可复用 H.264/AAC MP4 预览缓存
- [x] 4.5 增加下载与预览测试，覆盖重复消费者、摘要/大小不符、中断残留清理、缓存复用、H.264 直读、HEVC 转码和 FFmpeg 能力缺失

## 5. 转场匹配 Agent

- [x] 5.1 实现边界上下文构建器，只截取有界前后 ASR、主播标签和本地素材语义字段，并用确定性召回限制每次发送的候选数量
- [x] 5.2 扩展现有 DeepSeek Provider，增加无工具权限的转场匹配 Prompt、严格结构化响应 DTO 和批量/单边界执行入口
- [x] 5.3 在 Rust 中校验边界、素材键、版本、候选归属、渲染模式、置信度和 `none`，拒绝模型虚构素材及任意渲染参数
- [x] 5.4 实现固定 `0.75` 阈值：高置信度自动写入未锁边界并触发下载，低置信度只保存建议，Provider 失败不清除既有选择
- [x] 5.5 实现手动应用/替换/人工空值锁、解除锁和显式重新匹配，确保批量 Agent 永不覆盖 `manuallyLocked=true`
- [x] 5.6 实现片段重排、插入和删除后的稳定边界重算，仅局部失效受影响自动结果且不自动调用 LLM
- [x] 5.7 增加 Agent 与隐私测试，覆盖 Prompt 字段白名单、无有效边界、空目录、合法/低分/`none`/虚构响应、人工锁和局部失效

## 6. 剪辑工程时间模型与导出

- [x] 6.1 扩展剪辑工程 DTO 和 Tauri commands，返回边界素材、选择来源、置信度、理由、锁定、失效、下载及预览状态
- [x] 6.2 把已就绪 `bridge` 素材作为独立计划单元插入相邻原始片段之间，保留原声、默认 100% 音量并增加工程总时长
- [x] 6.3 统一播放器、时间轴和逐帧定位的工程时间映射，使桥接区间播放素材媒体，跨原始/桥接边界时保持现有播放状态语义
- [x] 6.4 更新工程字幕映射和逐字预览计划，让后续字幕按累计桥接时长顺延，并在桥接区间停止显示来源 ASR
- [x] 6.5 扩展冻结导出计划，按顺序裁取原始片段和完整桥接素材、统一画布/帧率/像素格式/音频格式并导出 H.264/AAC MP4
- [x] 6.6 在导出前校验固定素材版本全部已下载且通过完整性/解码检查；缺失或失败时阻止导出并提供素材级重试，不得静默跳过
- [x] 6.7 增加工程 repository、时间映射、字幕计划和 FFmpeg 导出测试，覆盖保存恢复、历史版本、重排、桥接时长、无字幕区间、取消及失败清理

## 7. 剪辑工作台界面

- [x] 7.1 在左侧素材库新增“转场素材”分类，展示缩略图/占位、标题、描述、标签、时长和下载/转码/就绪/失败状态
- [x] 7.2 实现只查询本地 SQLite DTO 的标题/描述/标签搜索及分类过滤，并提供预览、下载重试、拖放或点击应用操作
- [x] 7.3 在原始片段之间渲染尺寸稳定的桥接时间轴块，支持选中、预览、替换和移除，并正确参与缩放、滚动和播放头跟随
- [x] 7.4 扩展右侧属性面板，分别展示原始片段属性和桥接素材版本、匹配来源、分数、理由、人工锁与准备状态
- [x] 7.5 增加“智能匹配全部”、单边界重新匹配、解除人工锁和保留无转场交互，清晰展示低置信度建议及需要重新匹配状态
- [x] 7.6 保持现有桌面主题、固定工作台尺寸和页面局部滚动，补齐加载、空目录、最低版本、离线、下载失败和 Agent 失败状态
- [x] 7.7 增加 React 交互测试，覆盖本地搜索、缺图、按需预览、人工覆盖、批量匹配跳过锁定、时间轴桥接选择及字幕空白区间

## 8. 跨平台资源与端到端验证

- [x] 8.1 扩展 macOS/Windows FFmpeg 资源审计，验证 H.264、HEVC、AAC 解码及规范化、连接、H.264/AAC MP4 编码能力
- [x] 8.2 为受控小样增加 H.264 和 HEVC 带声桥接素材，验证预览转码与最终导出的画面、原声、总时长和字幕时间
- [x] 8.3 使用服务端示例的 33 条目录执行开发环境集成验证，覆盖 13 个 H.264、20 个 HEVC、32 个预览图和 1 个缺图素材，但不把远程媒体提交进仓库
- [x] 8.4 运行 Rust 单元/集成测试、前端测试、TypeScript 构建、客户端构建、资源审计、`openspec validate --strict` 和 `git diff --check`，修复所有回归
- [x] 8.5 更新 README 和开发手册中的 `0.3.0` 版本、素材 API 配置、本地缓存位置、同步/下载诊断、跨平台打包及人工验证步骤

## 1. 数据模型与兼容迁移

- [x] 1.1 为智能任务、不可变批次、阶段尝试、候选账本及来源区间、草稿代次建立 SQLite 迁移、索引、外键和状态约束，并增加迁移重复执行测试
- [x] 1.2 重建 `ai_clip_projects` 的单运行唯一约束，增加智能任务、草稿代次和所有权字段，并为普通工程建立等价的部分唯一索引
- [x] 1.3 新增 `ai_clip_project_sources`，为全部历史工程回填原 `highlight_run_id`，校验每个普通工程恰有一个来源并覆盖历史数据库升级测试
- [x] 1.4 在 Rust domain、repository 和 DTO 中定义智能任务、批次、阶段尝试、候选来源、草稿快照和终态枚举，拒绝非法状态转换
- [x] 1.5 为新表和跨来源关系增加 repository CRUD、事务竞态、级联删除与旧工程兼容测试

## 2. 剪辑工程多来源与版本保护

- [x] 2.1 将工程创建、候选追加和字幕副本构建的归属校验改为使用 `ai_clip_project_sources`，同时保持普通单运行工程行为不变
- [x] 2.2 修改工程详情、预览计划、字幕映射、转场上下文和导出来源查询，使合辑可读取多个关联高光运行且拒绝集合外候选或输入
- [x] 2.3 实现草稿自动化/用户所有权转换，在任一人工工程 mutation 的同一事务中锁定当前代次并阻止迟到自动更新
- [x] 2.4 实现导出冻结快照，原子固定任务、草稿代次、工程版本、来源、片段、字幕和桥接素材，并在导出期间拒绝自动化与人工 mutation
- [x] 2.5 增加普通工程恢复、多运行合辑创建、集合外来源拒绝、人工编辑竞态、导出竞态及导出后下一代草稿测试

## 3. 智能编排器基础

- [x] 3.1 新增 Rust `SmartClippingWorkflow` 服务和显式状态机，由父任务持有模式、受信来源、授权快照、配置指纹、阶段和活动草稿代次
- [x] 3.2 实现本地文件选择引用和直播会话选择的后端校验，拒绝任意路径、任意直播 URL、非监听会话及活动媒体文件
- [x] 3.3 实现任务级或直播场次级一次性授权摘要，并在激活、Provider、模型、来源或文本发送范围改变时使授权失效
- [x] 3.4 实现阶段尝试的输入指纹、尝试代次、结果引用、脱敏错误和幂等复用逻辑，确保未变化的成功阶段不会重复执行
- [x] 3.5 实现父任务取消、阶段局部重试和应用重启恢复，隔离旧代次事件及迟到 Provider 响应，并确保取消令牌不传播给 recorder
- [x] 3.6 增加状态转换、授权作用域、缓存命中、失败阶段重试、重启恢复、旧事件隔离和录制独立性单元测试

## 4. 第一阶段：本地视频一键剪辑

- [x] 4.1 为本地模式冻结一个有序输入批次，复用资源、内存、磁盘预检和既有全局单并发 ASR 队列
- [x] 4.2 在本地批次 ASR 完成后验证一次性授权并自动启动既有高光工作流，同时保持普通 AI 项目等待显式授权
- [x] 4.3 按运行冻结的合格和优秀阈值保存全部合法候选、自动选择优秀候选，并在无优秀候选时进入候选审阅而不创建空工程
- [x] 4.4 从自动入选候选创建一个智能合辑草稿，复制入选片段的工程字幕并发布可恢复的草稿代次
- [x] 4.5 在自动化拥有且版本匹配的草稿上调用既有保守文本纠错，确保只发送未隐藏且仍等于 ASR 原文的工程字幕副本
- [x] 4.6 在纠错后调用既有两阶段转场 Agent，复用素材目录和冻结阈值，高分自动应用、低分只建议，目录不可用时保留可审阅草稿
- [x] 4.7 增加本地模式从选择文件到可审阅草稿的集成测试，覆盖普通手动流程不自动运行、无优秀候选、纠错失败、转场失败和显式导出门禁

## 5. Tauri 命令、事件与前端数据层

- [x] 5.1 增加创建、授权、列出、查询、取消、阶段重试和打开草稿的 Tauri commands，并在 command 层执行激活、来源和状态门禁
- [x] 5.2 定义带任务 ID、任务代次、批次 ID、阶段、单调序号和脱敏错误的 `ai-smart-workflow-event`，在 repository 提交后发布
- [x] 5.3 在 TypeScript API 和类型中镜像命令、任务快照、批次、阶段、草稿和事件结构，并拒绝浏览器演示发起真实媒体或 Provider 处理
- [x] 5.4 实现页面挂载、重连和重启后的“先查询快照、再订阅事件”恢复路径，并过滤旧代次或旧序号事件
- [x] 5.5 增加 command 参数校验、事件顺序、重挂载恢复和脱敏错误序列化测试

## 6. 智能成片桌面体验

- [x] 6.1 在 AI 剪辑首页增加“智能成片”主入口、任务列表和待审阅草稿，同时将既有普通 AI 项目保留为可访问的高级模式
- [x] 6.2 实现本地视频/直播间分段模式创建流程，本地模式使用系统文件选择器，直播模式只展示正在录制的受信会话且不提供 URL 输入
- [x] 6.3 实现一次性授权确认界面，展示来源、目标、输出偏好、Provider、模型、最小文本范围及激活/资源门禁，不暴露 Key 或原始响应
- [x] 6.4 实现任务详情中的阶段进度、批次和积压、候选与采用统计、脱敏错误、取消、局部重试及可审阅入口
- [x] 6.5 在智能草稿和工作台展示自动更新、人工拥有、导出冻结及下一代草稿状态，并保持导出必须由用户明确选择目标和触发
- [x] 6.6 为宽窄桌面窗口、长错误、加载/空/失败状态、键盘与辅助技术操作补齐样式和组件测试，确保文字、按钮和进度不重叠

## 7. 第二阶段：直播分片增量处理

- [x] 7.1 订阅 recorder 的完成分片登记，在校验会话、完成状态、视频库 ID 和源指纹后按稳定游标幂等创建冻结批次
- [x] 7.2 将直播批次接入既有 ASR 队列并实现有界积压处理，按源时间稳定调度、不丢弃分片、不读取活动文件且不抢占录制
- [x] 7.3 为高光发现构造相邻完整分片的有限句段上下文，校验所有候选回指真实稳定句段和已登记源内区间
- [x] 7.4 实现跨批次候选账本，按核心句段、规范化会话时间和受控语义指纹合并重复候选，并支持跨分片逻辑候选映射为连续工程片段
- [x] 7.5 在新唯一优秀候选到达时事务更新自动化拥有的活动草稿；遇到人工编辑、导出或版本冲突时创建或更新下一草稿代次
- [x] 7.6 在直播结束后处理游标内剩余完成分片并收敛任务终态，保留所有草稿、普通子项目和失败批次供审阅或重试
- [x] 7.7 增加重复事件、乱序事件、跨分片高光、候选去重、处理积压、单批失败、应用重启、人工编辑、导出期间新高光及直播结束的集成测试

## 8. 隐私指标、发行与验收

- [x] 8.1 实现隐私安全的漏斗和耗时指标，只记录模式、平台、阶段终态、桶化耗时、批次、候选、采用、首稿和导出聚合数据
- [x] 8.2 增加遥测负面测试，证明媒体、音频、字幕、理由、主播或直播间标识、本地路径、Prompt、凭据、原始响应和内容指纹不会上报
- [x] 8.3 计算从相关最后分片完成登记到草稿可审阅的直播首稿延迟，增加 10 分钟目标的边界、积压和时钟测试
- [ ] 8.4 在 macOS arm64、macOS Intel x86_64 和 Windows x64 验证随包 FFmpeg/FFprobe、Whisper、ASR/VAD 模型的架构、完整性、识别、预览和 H.264/AAC 导出能力
  - macOS arm64 已在真实目标机使用隔离构建的 `.app` 随包资源通过架构、完整性、VAD、Whisper 中文识别、应用预览和 H.264/AAC 导出；证据见 `verification/8.4-platform-release-validation.md`。
  - 阻塞：仍缺真实 Intel Mac 和 Windows x64 目标机证据；不得用 Rosetta、交叉编译、开发机系统二进制或契约测试替代。
- [x] 8.5 执行本地与直播端到端验收，覆盖选择来源、ASR、高光、自动选择、工程字幕纠错、转场匹配、人工审阅、显式导出、恢复和下一草稿版本
- [x] 8.6 回归普通 AI 项目、已结束直播导入、手动高光授权、人工候选选择、一键纠错、一键 Agent、历史工程和既有 MP4 导出，确认智能授权不会扩散
- [x] 8.7 更新用户文档、运行资源发行清单和故障诊断说明，明确两种模式、任务级授权、仅处理完成分片、显式导出及目标平台边界
